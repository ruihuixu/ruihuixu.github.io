**[《代码随想录》作者：程序员Carl](https://github.com/youngyangyang04)**


代码随想录官⽹ [（网站持续更新优化内容，建议直接看网站）](https://www.programmercarl.com/) ：www.programmercarl.com

[代码随想录Github开源地址](https://github.com/youngyangyang04/leetcode-master)

[代码随想录算法公开课](https://programmercarl.com/other/gongkaike.html) [，代码随想录的全部内容将由我（程序员Carl）视频讲解并开免费开放给⼤家。](https://github.com/youngyangyang04)


[《代码随想录》](https://programmercarl.com/other/publish.html) 已经出版。


[代码随想录知识星球](https://programmercarl.com/other/kstar.html) 上万录友在这⾥学习


[代码随想录算法训练营](https://programmercarl.com/other/xunlianying.html) 帮助录友⾼效刷完代码随想录。


[微信公众号：代码随想录](https://img-blog.csdnimg.cn/74f47bc466234462bd2acc3ba6cd1b70.png)


[组队刷题，可以添加代码随想录官⽅微信](https://www.programmercarl.com/other/wechat.html)


[ACM模式练习，推荐：卡码⽹](https://kamacoder.com/)


**特别提示** ：PDF仅提供C++语⾔版本同时PDF中很多动图⽆法加载，其他编程语⾔版本和查看动图可以移步⾄代码

随想录官⽅⽹站查看。


来看看栈和队列不为⼈知的⼀⾯
# **1. 栈与队列理论基础**


我想栈和队列的原理⼤家应该很熟悉了，队列是先进先出，栈是先进后出。


如图所示：


那么我这⾥再列出四个关于栈的问题，⼤家可以思考⼀下。以下是以C++为例，使⽤其他编程语⾔的同学也对应思

考⼀下，⾃⼰使⽤的编程语⾔⾥栈和队列是什么样的。


1. C++中stack 是容器么？

2. 我们使⽤的stack是属于哪个版本的STL？

3. 我们使⽤的STL中stack是如何实现的？

4. stack 提供迭代器来遍历stack空间么？


相信这四个问题并不那么好回答， 因为⼀些同学使⽤数据结构会停留在⾮常表⾯上的应⽤，稍稍往深⼀问，就会有

好像懂，好像也不懂的感觉。


有的同学可能仅仅知道有栈和队列这么个数据结构，却不知道底层实现，也不清楚所使⽤栈和队列和STL是什么关

系。


所以这⾥我再给⼤家扫⼀遍基础知识，


⾸先⼤家要知道 栈和队列是STL（C++标准库）⾥⾯的两个数据结构。


C++标准库是有多个版本的，要知道我们使⽤的STL是哪个版本，才能知道对应的栈和队列的实现原理。


那么来介绍⼀下，三个最为普遍的STL版本：


1. HP STL

其他版本的C++ STL，⼀般是以HP STL为蓝本实现出来的，HP STL是C++ STL的第⼀个实现版本，⽽且开放源

代码。


2. P.J.Plauger STL

由P.J.Plauger参照HP STL实现出来的，被Visual C++编译器所采⽤，不是开源的。

3. SGI STL

由Silicon Graphics Computer Systems公司参照HP STL实现，被Linux的C++编译器GCC所采⽤，SGI STL是

开源软件，源码可读性甚⾼。


接下来介绍的栈和队列也是SGI STL⾥⾯的数据结构， 知道了使⽤版本，才知道对应的底层实现。


来说⼀说栈，栈先进后出，如图所示：


栈提供push 和 pop 等等接⼝，所有元素必须符合先进后出规则，所以栈不提供⾛访功能，也不提供迭代器

(iterator)。 不像是set 或者map 提供迭代器iterator来遍历所有元素。


**栈是以底层容器完成其所有的⼯作，对外提供统⼀的接⼝，底层容器是可插拔的（也就是说我们可以控制使⽤哪种**

**容器来实现栈的功能）。**


所以STL中栈往往不被归类为容器，⽽被归类为container adapter（容器适配器）。


那么问题来了，STL 中栈是⽤什么容器实现的？


从下图中可以看出，栈的内部结构，栈的底层实现可以是vector，deque，list 都是可以的， 主要就是数组和链表

的底层实现。


**我们常⽤的SGI STL，如果没有指定底层实现的话，默认是以deque为缺省情况下栈的底层结构。**


deque是⼀个双向队列，只要封住⼀段，只开通另⼀端就可以实现栈的逻辑了。


**SGI STL中** **队列底层实现缺省情况下⼀样使⽤deque实现的。**


我们也可以指定vector为栈的底层实现，初始化语句如下：





刚刚讲过栈的特性，对应的队列的情况是⼀样的。


队列中先进先出的数据结构，同样不允许有遍历⾏为，不提供迭代器, **SGI STL中队列⼀样是以deque为缺省情况下**

**的底部结构。**


也可以指定list 为起底层实现，初始化queue的语句如下：





所以STL 队列也不被归类为容器，⽽被归类为container adapter（ 容器适配器）。


我这⾥讲的都是C++ 语⾔中的情况， 使⽤其他语⾔的同学也要思考栈与队列的底层实现问题， 不要对数据结构的

使⽤浅尝辄⽌，⽽要深挖其内部原理，才能夯实基础。


⼯作上⼀定没⼈这么搞，但是考察对栈、队列理解程度的好题
# **2.⽤栈实现队列**


[⼒扣题⽬链接](https://leetcode.cn/problems/implement-queue-using-stacks/)


使⽤栈实现队列的下列操作：


push(x) -- 将⼀个元素放⼊队列的尾部。

pop() -- 从队列⾸部移除元素。

peek() -- 返回队列⾸部的元素。

empty() -- 返回队列是否为空。


示例:





说明:


你只能使⽤标准的栈操作 -- 也就是只有 push to top, peek/pop from top, size, 和 is empty 操作是合法的。

你所使⽤的语⾔也许不⽀持栈。你可以使⽤ list 或者 deque（双端队列）来模拟⼀个栈，只要是标准的栈操

作即可。


假设所有操作都是有效的 （例如，⼀个空的队列不会调⽤ pop 或者 peek 操作）。

## **算法公开课**


**[《代码随想录》算法视频公开课：栈的基本操作！ | LeetCode：232.⽤栈实现队列，相信结合视频再看本篇题](https://programmercarl.com/other/gongkaike.html)**

**解，更有助于⼤家对本题的理解** 。

## **思路**


这是⼀道模拟题，不涉及到具体算法，考察的就是对栈和队列的掌握程度。


使⽤栈来模式队列的⾏为，如果仅仅⽤⼀个栈，是⼀定不⾏的，所以需要两个栈 **⼀个输⼊栈，⼀个输出栈** ，这⾥要

注意输⼊栈和输出栈的关系。


下⾯动画模拟以下队列的执⾏过程：


执⾏语句：

queue.push(1);

queue.push(2);

queue.pop(); **注意此时的输出栈的操作**

queue.push(3);

queue.push(4);

queue.pop();


queue.pop(); **注意此时的输出栈的操作**

queue.pop();

queue.empty();


在push数据的时候，只要数据放进输⼊栈就好， **但在pop的时候，操作就复杂⼀些，输出栈如果为空，就把进栈数**

**据全部导⼊进来（注意是全部导⼊）** ，再从出栈弹出数据，如果输出栈不为空，则直接从出栈弹出数据就可以了。


最后如何判断队列为空呢？ **如果进栈和出栈都为空的话，说明模拟的队列为空了。**


在代码实现的时候，会发现pop() 和 peek()两个函数功能类似，代码实现上也是类似的，可以思考⼀下如何把代码

抽象⼀下。


C++代码如下：


class MyQueue {


public:


stack<int> stIn;


stack<int> stOut;


/** Initialize your data structure here. */


MyQueue() {


}


/** Push element x to the back of queue. */


void push(int x) {


stIn.push(x);


}


/** Removes the element from in front of queue and returns that element. */


int pop() {

// 只有当stOut为空的时候，再从stIn⾥导⼊数据（导⼊stIn全部数据）


if (stOut.empty()) {

// 从stIn导⼊数据直到stIn为空


while(!stIn.empty()) {


stOut.push(stIn.top());


stIn.pop();


}


}


int result = stOut.top();


stOut.pop();


return result;


}


/** Get the front element. */


int peek() {

int res = this->pop(); // 直接使⽤已有的pop函数

stOut.push(res); // 因为pop函数弹出了元素res，所以再添加回去


return res;


}


/** Returns whether the queue is empty. */


bool empty() {


return stIn.empty() && stOut.empty();


}


};


时间复杂度: push和empty为O(1), pop和peek为O(n)

空间复杂度: O(n)

## **拓展**


可以看出peek()的实现，直接复⽤了pop()， 要不然，对stOut判空的逻辑⼜要重写⼀遍。


再多说⼀些代码开发上的习惯问题，在⼯业级别代码开发中，最忌讳的就是 实现⼀个类似的函数，直接把代码粘过

来改⼀改就完事了。


这样的项⽬代码会越来越乱， **⼀定要懂得复⽤，功能相近的函数要抽象出来，不要⼤量的复制粘贴，很容易出问**

**题！（踩过坑的⼈⾃然懂）**


⼯作中如果发现某⼀个功能⾃⼰要经常⽤，同事们可能也会⽤到，⾃⼰就花点时间把这个功能抽象成⼀个好⽤的函

数或者⼯具类，不仅⾃⼰⽅便，也⽅便了同事们。


同事们就会逐渐认可你的⼯作态度和⼯作能⼒，⾃⼰的⼝碑都是这么⼀点⼀点积累起来的！在同事圈⾥⼝碑起来了

之后，你就发现⾃⼰⾛上了⼀个正循环，以后的升职加薪才少不了你！哈哈哈


⽤队列实现栈还是有点别扭。
# **3. ⽤队列实现栈**


[⼒扣题⽬链接](https://leetcode.cn/problems/implement-stack-using-queues/)


使⽤队列实现栈的下列操作：


push(x) -- 元素 x ⼊栈

pop() -- 移除栈顶元素

top() -- 获取栈顶元素

empty() -- 返回栈是否为空


注意:


你只能使⽤队列的基本操作-- 也就是 push to back, peek/pop from front, size, 和 is empty 这些操作是合法

的。


你所使⽤的语⾔也许不⽀持队列。 你可以使⽤ list 或者 deque（双端队列）来模拟⼀个队列, 只要是标准的

队列操作即可。


你可以假设所有操作都是有效的（例如, 对⼀个空的栈不会调⽤ pop 或者 top 操作）。

## **算法公开课**


**[《代码随想录》算法视频公开课：队列的基本操作！ | LeetCode：225. ⽤队列实现栈，相信结合视频再看本篇题](https://programmercarl.com/other/gongkaike.html)**

**解，更有助于⼤家对本题的理解** 。

## **思路**


（这⾥要强调是单向队列）


有的同学可能疑惑这种题⽬有什么实际⼯程意义， **其实很多算法题⽬主要是对知识点的考察和教学意义远⼤于其⼯**

**程实践的意义，所以⾯试题也是这样！**


[刚刚做过栈与队列：我⽤栈来实现队列怎么样？的同学可能依然想着⽤⼀个输⼊队列，⼀个输出队列，就可以模拟](https://programmercarl.com/0232.%E7%94%A8%E6%A0%88%E5%AE%9E%E7%8E%B0%E9%98%9F%E5%88%97.html)

栈的功能，仔细想⼀下还真不⾏！


**队列模拟栈，其实⼀个队列就够了** ，那么我们先说⼀说两个队列来实现栈的思路。


**队列是先进先出的规则，把⼀个队列中的数据导⼊另⼀个队列中，数据的顺序并没有变，并没有变成先进后出的顺**

**序。**


所以⽤栈实现队列， 和⽤队列实现栈的思路还是不⼀样的，这取决于这两个数据结构的性质。


但是依然还是要⽤两个队列来模拟栈，只不过没有输⼊和输出的关系，⽽是另⼀个队列完全⽤来备份的！


如下⾯动画所示， **⽤两个队列que1和que2实现队列的功能，que2其实完全就是⼀个备份的作⽤** ，把que1最后⾯

的元素以外的元素都备份到que2，然后弹出最后⾯的元素，再把其他元素从que2导回que1。


模拟的队列执⾏语句如下：





详细如代码注释所示：


class MyStack {


public:


queue<int> que1;

queue<int> que2; // 辅助队列，⽤来备份


/** Initialize your data structure here. */


MyStack() {


}


/** Push element x onto stack. */


void push(int x) {


que1.push(x);


}


/** Removes the element on top of the stack and returns that element. */


int pop() {


int size = que1.size();


size--;

while (size--) { // 将que1 导⼊que2，但要留下最后⼀个元素


que2.push(que1.front());


que1.pop();


}


int result = que1.front(); // 留下的最后⼀个元素就是要返回的值


que1.pop();

que1 = que2;      // 再将que2赋值给que1

while (!que2.empty()) { // 清空que2


que2.pop();


}


return result;


}


/** Get the top element. */


int top() {


return que1.back();


}


/** Returns whether the stack is empty. */


bool empty() {


return que1.empty();


}


};


时间复杂度: push为O(n)，其他为O(1)

空间复杂度: O(n)

## **优化**


其实这道题⽬就是⽤⼀个队列就够了。


**⼀个队列在模拟栈弹出元素的时候只要将队列头部的元素（除了最后⼀个元素外）** **重新添加到队列尾部，此时再去**

**弹出元素就是栈的顺序了。**


C++优化代码





时间复杂度: push为O(n)，其他为O(1)

空间复杂度: O(n)


数据结构与算法应⽤往往隐藏在我们看不到的地⽅
# **4. 有效的括号**


[⼒扣题⽬链接](https://leetcode.cn/problems/valid-parentheses/)


给定⼀个只包括 '('，')'，'{'，'}'，'['，']' 的字符串，判断字符串是否有效。


有效字符串需满⾜：


左括号必须⽤相同类型的右括号闭合。


左括号必须以正确的顺序闭合。


注意空字符串可被认为是有效字符串。


示例 1:


输⼊: "()"

输出: true


示例 2:


输⼊: "()[]{}"

输出: true


示例 3:


输⼊: "(]"

输出: false


示例 4:


输⼊: "([)]"

输出: false


示例 5:


输⼊: "{[]}"

输出: true

## **算法公开课**


**[《代码随想录》算法视频公开课：栈的拿⼿好戏！| LeetCode：20. 有效的括号，相信结合视频再看本篇题解，更](https://programmercarl.com/other/gongkaike.html)**

**有助于⼤家对本题的理解** 。

## **思路**

### **题外话**


**括号匹配是使⽤栈解决的经典问题。**


题意其实就像我们在写代码的过程中，要求括号的顺序是⼀样的，有左括号，相应的位置必须要有右括号。


如果还记得编译原理的话，编译器在 词法分析的过程中处理括号、花括号等这个符号的逻辑，也是使⽤了栈这种数

据结构。


再举个例⼦，linux系统中，cd这个进⼊⽬录的命令我们应该再熟悉不过了。


这个命令最后进⼊a⽬录，系统是如何知道进⼊了a⽬录呢 ，这就是栈的应⽤（其实可以出⼀道相应的⾯试题了）


所以栈在计算机领域中应⽤是⾮常⼴泛的。


有的同学经常会想学的这些数据结构有什么⽤，也开发不了什么软件，⼤多数同学说的软件应该都是可视化的软件

例如APP、⽹站之类的，那都是⾮常上层的应⽤了，底层很多功能的实现都是基础的数据结构和算法。


**所以数据结构与算法的应⽤往往隐藏在我们看不到的地⽅！**


这⾥我就不过多展开了，先来看题。

### **进⼊正题**


由于栈结构的特殊性，⾮常适合做对称匹配类的题⽬。


⾸先要弄清楚，字符串⾥的括号不匹配有⼏种情况。


**⼀些同学，在⾯试中看到这种题⽬上来就开始写代码，然后就越写越乱。**


建议在写代码之前要分析好有哪⼏种不匹配的情况，如果不在动⼿之前分析好，写出的代码也会有很多问题。


先来分析⼀下 这⾥有三种不匹配的情况，


1. 第⼀种情况，字符串⾥左⽅向的括号多余了 ，所以不匹配。


2. 第⼆种情况，括号没有多余，但是 括号的类型没有匹配上。


3. 第三种情况，字符串⾥右⽅向的括号多余了，所以不匹配。


我们的代码只要覆盖了这三种不匹配的情况，就不会出问题，可以看出 动⼿之前分析好题⽬的重要性。


动画如下：


第⼀种情况：已经遍历完了字符串，但是栈不为空，说明有相应的左括号没有右括号来匹配，所以return false


第⼆种情况：遍历字符串匹配的过程中，发现栈⾥没有要匹配的字符。所以return false


第三种情况：遍历字符串匹配的过程中，栈已经为空了，没有匹配的字符了，说明右括号没有找到对应的左括号

return false


那么什么时候说明左括号和右括号全都匹配了呢，就是字符串遍历完之后，栈是空的，就说明全都匹配了。


分析完之后，代码其实就⽐较好写了，


但还有⼀些技巧，在匹配左括号的时候，右括号先⼊栈，就只需要⽐较当前元素和栈顶相不相等就可以了，⽐左括

号先⼊栈代码实现要简单的多了！


实现C++代码如下：


class Solution {


public:


bool isValid(string s) {

if (s.size() % 2 != 0) return false; // 如果s的⻓度为奇数，⼀定不符合要求


stack<char> st;


for (int i = 0; i < s.size(); i++) {


if (s[i] == '(') st.push(')');


else if (s[i] == '{') st.push('}');


else if (s[i] == '[') st.push(']');


// 第三种情况：遍历字符串匹配的过程中，栈已经为空了，没有匹配的字符了，说明右括号没有找到

对应的左括号 return false

// 第⼆种情况：遍历字符串匹配的过程中，发现栈⾥没有我们要匹配的字符。所以return false


else if (st.empty() || st.top() != s[i]) return false;

else st.pop(); // st.top() 与 s[i]相等，栈弹出元素


}

// 第⼀种情况：此时我们已经遍历完了字符串，但是栈不为空，说明有相应的左括号没有右括号来匹配，所

以return false，否则就return true


return st.empty();


}


};


时间复杂度: O(n)

空间复杂度: O(n)


技巧性的东⻄没有固定的学习⽅法，还是要多看多练，⾃⼰灵活运⽤了。


匹配问题都是栈的强项
# **5. 删除字符串中的所有相邻重复项**


[⼒扣题⽬链接](https://leetcode.cn/problems/remove-all-adjacent-duplicates-in-string/)


给出由⼩写字⺟组成的字符串 S，重复项删除操作会选择两个相邻且相同的字⺟，并删除它们。


在 S 上反复执⾏重复项删除操作，直到⽆法继续删除。


在完成所有重复项删除操作后返回最终的字符串。答案保证唯⼀。


示例：


输⼊："abbaca"

输出："ca"

解释：例如，在 "abbaca" 中，我们可以删除 "bb" 由于两字⺟相邻且相同，这是此时唯⼀可以执⾏删除操作

的重复项。之后我们得到字符串 "aaca"，其中⼜只有 "aa" 可以执⾏重复项删除操作，所以最后的字符串为

"ca"。


提示：


1 <= S.length <= 20000

S 仅由⼩写英⽂字⺟组成。

## **算法公开课**


**[《代码随想录》算法视频公开课：栈的好戏还要继续！| LeetCode：1047. 删除字符串中的所有相邻重复项，相信](https://programmercarl.com/other/gongkaike.html)**

**结合视频再看本篇题解，更有助于⼤家对本题的理解** 。

## **思路**

### **正题**


[本题要删除相邻相同元素，相对于20. 有效的括号来说其实也是匹配问题，20. 有效的括号](https://programmercarl.com/0020.%E6%9C%89%E6%95%88%E7%9A%84%E6%8B%AC%E5%8F%B7.html) 是匹配左右括号，本题

是匹配相邻元素，最后都是做消除的操作。


本题也是⽤栈来解决的经典题⽬。


那么栈⾥应该放的是什么元素呢？


我们在删除相邻重复项的时候，其实就是要知道当前遍历的这个元素，我们在前⼀位是不是遍历过⼀样数值的元

素，那么如何记录前⾯遍历过的元素呢？


所以就是⽤栈来存放，那么栈的⽬的，就是存放遍历过的元素，当遍历当前的这个元素的时候，去栈⾥看⼀下我们

是不是遍历过相同数值的相邻元素。


然后再去做对应的消除操作。 如动画所示：


从栈中弹出剩余元素，此时是字符串ac，因为从栈⾥弹出的元素是倒序的，所以再对字符串进⾏反转⼀下，就得到

了最终的结果。


C++代码 :


class Solution {


public:


string removeDuplicates(string S) {


stack<char> st;


for (char s : S) {


if (st.empty() || s != st.top()) {


st.push(s);


} else {

st.pop(); // s 与 st.top()相等的情况


}


}


string result = "";

while (!st.empty()) { // 将栈中元素放到result字符串汇总


result += st.top();


st.pop();


}

reverse (result.begin(), result.end()); // 此时字符串需要反转⼀下


return result;


}


};


时间复杂度: O(n)

空间复杂度: O(n)


当然可以拿字符串直接作为栈，这样省去了栈还要转为字符串的操作。


代码如下：


时间复杂度: O(n)

空间复杂度: O(1)，返回值不计空间复杂度


### **题外话**

这道题⽬就像是我们玩过的游戏对对碰，如果相同的元素挨在⼀起就要消除。


可能我们在玩游戏的时候感觉理所当然应该消除，但程序⼜怎么知道该如何消除呢，特别是消除之后⼜有新的元素

可能挨在⼀起。


此时游戏的后端逻辑就可以⽤⼀个栈来实现（我没有实际考察对对碰或者爱消除游戏的代码实现，仅从原理上进⾏

推断）。


游戏开发可能使⽤栈结构，编程语⾔的⼀些功能实现也会使⽤栈结构，实现函数递归调⽤就需要栈，但不是每种编

程语⾔都⽀持递归，例如：


**递归的实现就是：每⼀次递归调⽤都会把函数的局部变量、参数值和返回地址等压⼊调⽤栈中** ，然后递归返回的时

候，从栈顶弹出上⼀次递归的各项参数，所以这就是递归为什么可以返回上⼀层位置的原因。


相信⼤家应该遇到过⼀种错误就是栈溢出，系统输出的异常是 Segmentation fault （当然不是所有的

Segmentation fault 都是栈溢出导致的） ，如果你使⽤了递归，就要想⼀想是不是⽆限递归了，那么系统调⽤

栈就会溢出。


⽽且 **在企业项⽬开发中，尽量不要使⽤递归** ！在项⽬⽐较⼤的时候，由于参数多，全局变量等等，使⽤递归很容易

判断不充分return的条件，⾮常容易⽆限递归（或者递归层级过深）， **造成栈溢出错误（这种问题还不好排查！）**


这不仅仅是⼀道好题，也展现出计算机的思考⽅式
# **6. 逆波兰表达式求值**


[⼒扣题⽬链接](https://leetcode.cn/problems/evaluate-reverse-polish-notation/)


根据 逆波兰表示法，求表达式的值。


有效的运算符包括 +, -, *, / 。每个运算对象可以是整数，也可以是另⼀个逆波兰表达式。


说明：


整数除法只保留整数部分。

给定逆波兰表达式总是有效的。换句话说，表达式总会得出有效数值且不存在除数为 0 的情况。


示例 1：


输⼊: ["2", "1", "+", "3", " * "]

输出: 9

解释: 该算式转化为常⻅的中缀算术表达式为：((2 + 1) * 3) = 9


示例 2：


输⼊: ["4", "13", "5", "/", "+"]

输出: 6


解释: 该算式转化为常⻅的中缀算术表达式为：(4 + (13 / 5)) = 6


示例 3：


输⼊: ["10", "6", "9", "3", "+", "-11", " * ", "/", " * ", "17", "+", "5", "+"]

输出: 22

解释:该算式转化为常⻅的中缀算术表达式为：





逆波兰表达式：是⼀种后缀表达式，所谓后缀就是指运算符写在后⾯。


平常使⽤的算式则是⼀种中缀表达式，如 ( 1 + 2 ) * ( 3 + 4 ) 。


该算式的逆波兰表达式写法为 ( ( 1 2 + ) ( 3 4 + ) * ) 。


逆波兰表达式主要有以下两个优点：


去掉括号后表达式⽆歧义，上式即便写成 1 2 + 3 4 + * 也可以依据次序计算出正确结果。


适合⽤栈操作运算：遇到数字则⼊栈；遇到运算符则取出栈顶两个数字进⾏计算，并将结果压⼊栈中。

## **算法公开课**


**[《代码随想录》算法视频公开课：栈的最后表演！ | LeetCode：150. 逆波兰表达式求值，相信结合视频再看本篇](https://programmercarl.com/other/gongkaike.html)**

**题解，更有助于⼤家对本题的理解** 。

## **思路**

### **正题**


[在上⼀篇⽂章中1047.删除字符串中的所有相邻重复项提到了](https://programmercarl.com/1047.%E5%88%A0%E9%99%A4%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E6%89%80%E6%9C%89%E7%9B%B8%E9%82%BB%E9%87%8D%E5%A4%8D%E9%A1%B9.html) 递归就是⽤栈来实现的。


所以 **栈与递归之间在某种程度上是可以转换的！** 这⼀点我们在后续讲解⼆叉树的时候，会更详细的讲解到。


那么来看⼀下本题， **其实逆波兰表达式相当于是⼆叉树中的后序遍历** 。 ⼤家可以把运算符作为中间节点，按照后序

遍历的规则画出⼀个⼆叉树。


但我们没有必要从⼆叉树的⻆度去解决这个问题，只要知道逆波兰表达式是⽤后序遍历的⽅式把⼆叉树序列化了，

就可以了。


在进⼀步看，本题中每⼀个⼦表达式要得出⼀个结果，然后拿这个结果再进⾏运算，那么 **这岂不就是⼀个相邻字符**

**[串消除的过程，和1047.删除字符串中的所有相邻重复项中的对对碰游戏是不是就⾮常像了。](https://programmercarl.com/1047.%E5%88%A0%E9%99%A4%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E6%89%80%E6%9C%89%E7%9B%B8%E9%82%BB%E9%87%8D%E5%A4%8D%E9%A1%B9.html)**


如动画所示：


[相信看完动画⼤家应该知道，这和1047. 删除字符串中的所有相邻重复项是差不错的，只不过本题不要相邻元素做](https://programmercarl.com/1047.%E5%88%A0%E9%99%A4%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E6%89%80%E6%9C%89%E7%9B%B8%E9%82%BB%E9%87%8D%E5%A4%8D%E9%A1%B9.html)

消除了，⽽是做运算！


C++代码如下：


class Solution {


public:


int evalRPN(vector<string>& tokens) {

// ⼒扣修改了后台测试数据，需要⽤longlong


stack<long long> st;


for (int i = 0; i < tokens.size(); i++) {


if (tokens[i] == "+" || tokens[i] == "-" || tokens[i] == "*" || tokens[i]


== "/") {


long long num1 = st.top();


st.pop();


long long num2 = st.top();


st.pop();


if (tokens[i] == "+") st.push(num2 + num1);


if (tokens[i] == "-") st.push(num2          - num1);


if (tokens[i] == "*") st.push(num2          - num1);


if (tokens[i] == "/") st.push(num2 / num1);


} else {


st.push(stoll(tokens[i]));


}


}


int result = st.top();

st.pop(); // 把栈⾥最后⼀个元素弹出（其实不弹出也没事）


return result;


}


};


时间复杂度: O(n)

空间复杂度: O(n)

### **题外话**


我们习惯看到的表达式都是中缀表达式，因为符合我们的习惯，但是中缀表达式对于计算机来说就不是很友好了。


例如：4 + 13 / 5，这就是中缀表达式，计算机从左到右去扫描的话，扫到13，还要判断13后⾯是什么运算符，还

要⽐较⼀下优先级，然后13还和后⾯的5做运算，做完运算之后，还要向前回退到 4 的位置，继续做加法，你说麻

不麻烦！


那么将中缀表达式，转化为后缀表达式之后：["4", "13", "5", "/", "+"] ，就不⼀样了，计算机可以利⽤栈来顺序处

理，不需要考虑优先级了。也不⽤回退了， **所以后缀表达式对计算机来说是⾮常友好的。**


可以说本题不仅仅是⼀道好题，也展现出计算机的思考⽅式。


在1970年代和1980年代，惠普在其所有台式和⼿持式计算器中都使⽤了RPN（后缀表达式），直到2020年代仍在

某些模型中使⽤了RPN。


参考维基百科如下：


During the 1970s and 1980s, Hewlett-Packard used RPN in all of their desktop and hand-held

calculators, and continued to use it in some models into the 2020s.


要⽤啥数据结构呢？
# **7. 滑动窗⼝最⼤值**


[⼒扣题⽬链接](https://leetcode.cn/problems/sliding-window-maximum/)


给定⼀个数组 nums，有⼀个⼤⼩为 k 的滑动窗⼝从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗⼝

内的 k 个数字。滑动窗⼝每次只向右移动⼀位。


返回滑动窗⼝中的最⼤值。


进阶：


你能在线性时间复杂度内解决此题吗？


提示：


1 <= nums.length <= 10^5

-10^4 <= nums[i] <= 10^4

1 <= k <= nums.length

## **算法公开课**


**[《代码随想录》算法视频公开课：单调队列正式登场！| LeetCode：239. 滑动窗⼝最⼤值，相信结合视频再看本](https://programmercarl.com/other/gongkaike.html)**

**篇题解，更有助于⼤家对本题的理解** 。

## **思路**


这是使⽤单调队列的经典题⽬。


难点是如何求⼀个区间⾥的最⼤值呢？ （这好像是废话），暴⼒⼀下不就得了。


暴⼒⽅法，遍历⼀遍的过程中每次从窗⼝中再找到最⼤的数值，这样很明显是O(n × k)的算法。


有的同学可能会想⽤⼀个⼤顶堆（优先级队列）来存放这个窗⼝⾥的k个数字，这样就可以知道最⼤的最⼤值是多

少了， **但是问题是这个窗⼝是移动的，⽽⼤顶堆每次只能弹出最⼤值，我们⽆法移除其他数值，这样就造成⼤顶堆**

**维护的不是滑动窗⼝⾥⾯的数值了。所以不能⽤⼤顶堆。**


此时我们需要⼀个队列，这个队列呢，放进去窗⼝⾥的元素，然后随着窗⼝的移动，队列也⼀进⼀出，每次移动之

后，队列告诉我们⾥⾯的最⼤值是什么。


这个队列应该⻓这个样⼦：





每次窗⼝移动的时候，调⽤que.pop(滑动窗⼝中移除元素的数值)，que.push(滑动窗⼝添加元素的数值)，然后

que.front()就返回我们要的最⼤值。


这么个队列⾹不⾹，要是有现成的这种数据结构是不是更⾹了！


**可惜了，没有！** **我们需要⾃⼰实现这么个队列。**


然后再分析⼀下，队列⾥的元素⼀定是要排序的，⽽且要最⼤值放在出队⼝，要不然怎么知道最⼤值呢。


但如果把窗⼝⾥的元素都放进队列⾥，窗⼝移动的时候，队列需要弹出元素。


那么问题来了，已经排序之后的队列 怎么能把窗⼝要移除的元素（这个元素可不⼀定是最⼤值）弹出呢。


⼤家此时应该陷⼊深思.....


**其实队列没有必要维护窗⼝⾥的所有元素，只需要维护有可能成为窗⼝⾥最⼤值的元素就可以了，同时保证队列⾥**

**的元素数值是由⼤到⼩的。**


那么这个维护元素单调递减的队列就叫做 **单调队列，即单调递减或单调递增的队列。C++中没有直接⽀持单调队**

**列，需要我们⾃⼰来实现⼀个单调队列**


**不要以为实现的单调队列就是** **对窗⼝⾥⾯的数进⾏排序，如果排序的话，那和优先级队列⼜有什么区别了呢。**


来看⼀下单调队列如何维护队列⾥的元素。


动画如下：


对于窗⼝⾥的元素{2, 3, 5, 1,4}，单调队列⾥只维护{5, 4} 就够了，保持单调队列⾥单调递减，此时队列出⼝元素就

是窗⼝⾥最⼤元素。


此时⼤家应该怀疑单调队列⾥维护着{5, 4} 怎么配合窗⼝进⾏滑动呢？


设计单调队列的时候，pop，和push操作要保持如下规则：


1. pop(value)：如果窗⼝移除的元素value等于单调队列的出⼝元素，那么队列弹出元素，否则不⽤任何操作

2. push(value)：如果push的元素value⼤于⼊⼝元素的数值，那么就将队列⼊⼝的元素弹出，直到push元素的

数值⼩于等于队列⼊⼝元素的数值为⽌


保持如上规则，每次窗⼝移动的时候，只要问que.front()就可以返回当前窗⼝的最⼤值。


为了更直观的感受到单调队列的⼯作过程，以题⽬示例为例，输⼊: nums = [1,3,-1,-3,5,3,6,7], 和 k = 3，动画如

下：


那么我们⽤什么数据结构来实现这个单调队列呢？


[使⽤deque最为合适，在⽂章栈与队列：来看看栈和队列不为⼈知的⼀⾯中，我们就提到了常⽤的queue在没有指](https://programmercarl.com/%E6%A0%88%E4%B8%8E%E9%98%9F%E5%88%97%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)

定容器的情况下，deque就是默认底层容器。


基于刚刚说过的单调队列pop和push的规则，代码不难实现，如下：


class MyQueue { //单调队列（从⼤到⼩）


public:

deque<int> que; // 使⽤deque来实现单调队列

// 每次弹出的时候，⽐较当前要弹出的数值是否等于队列出⼝元素的数值，如果相等则弹出。

// 同时pop之前判断队列当前是否为空。


void pop(int value) {


if (!que.empty() && value == que.front()) {


que.pop_front();


}


}

// 如果push的数值⼤于⼊⼝元素的数值，那么就将队列后端的数值弹出，直到push的数值⼩于等于队列⼊⼝元

素的数值为⽌。

// 这样就保持了队列⾥的数值是单调从⼤到⼩的了。


void push(int value) {


while (!que.empty() && value      - que.back()) {


que.pop_back();


}


que.push_back(value);


}

// 查询当前队列⾥的最⼤值 直接返回队列前端也就是front就可以了。


int front() {


return que.front();


}


};


这样我们就⽤deque实现了⼀个单调队列，接下来解决滑动窗⼝最⼤值的问题就很简单了，直接看代码吧。


C++代码如下：






}


};


时间复杂度: O(n)

空间复杂度: O(k)


再来看⼀下时间复杂度，使⽤单调队列的时间复杂度是 O(n)。


有的同学可能想了，在队列中 push元素的过程中，还有pop操作呢，感觉不是纯粹的O(n)。


其实，⼤家可以⾃⼰观察⼀下单调队列的实现，nums 中的每个元素最多也就被 push_back 和 pop_back 各⼀

次，没有任何多余操作，所以整体的复杂度还是 O(n)。


空间复杂度因为我们定义⼀个辅助队列，所以是O(k)。

## **扩展**


⼤家貌似对单调队列 都有⼀些疑惑，⾸先要明确的是，题解中单调队列⾥的pop和push接⼝，仅适⽤于本题哈。

单调队列不是⼀成不变的，⽽是不同场景不同写法，总之要保证队列⾥单调递减或递增的原则，所以叫做单调队

列。 不要以为本题中的单调队列实现就是固定的写法哈。


⼤家貌似对deque也有⼀些疑惑，C++中deque是stack和queue默认的底层实现容器（这个我们之前已经讲过

啦），deque是可以两边扩展的，⽽且deque⾥元素并不是严格的连续分布的。


前K个⼤数问题，⽼⽣常谈，不得不谈
# **8.前 K 个⾼频元素**


[⼒扣题⽬链接](https://leetcode.cn/problems/top-k-frequent-elements/)


给定⼀个⾮空的整数数组，返回其中出现频率前 k ⾼的元素。


示例 1:


输⼊: nums = [1,1,1,2,2,3], k = 2

输出: [1,2]


示例 2:


输⼊: nums = [1], k = 1

输出: [1]


提示：


你可以假设给定的 k 总是合理的，且 1 ≤ k ≤ 数组中不相同的元素的个数。

你的算法的时间复杂度必须优于 $O(n \log n)$, n 是数组的⼤⼩。

题⽬数据保证答案唯⼀，换句话说，数组中前 k 个⾼频元素的集合是唯⼀的。


你可以按任意顺序返回答案。

## **算法公开课**


**[《代码随想录》算法视频公开课：优先级队列正式登场！⼤顶堆、⼩顶堆该怎么⽤？| LeetCode：347.前 K 个⾼](https://programmercarl.com/other/gongkaike.html)**

**频元素，相信结合视频再看本篇题解，更有助于⼤家对本题的理解** 。

## **思路**


这道题⽬主要涉及到如下三块内容：


1. 要统计元素出现频率

2. 对频率排序

3. 找出前K个⾼频元素


⾸先统计元素出现的频率，这⼀类的问题可以使⽤map来进⾏统计。


然后是对频率进⾏排序，这⾥我们可以使⽤⼀种 容器适配器就是 **优先级队列** 。


什么是优先级队列呢？


其实 **就是⼀个披着队列外⾐的堆** ，因为优先级队列对外接⼝只是从队头取元素，从队尾添加元素，再⽆其他取元素

的⽅式，看起来就是⼀个队列。


⽽且优先级队列内部元素是⾃动依照元素的权值排列。那么它是如何有序排列的呢？


缺省情况下priority_queue利⽤max-heap（⼤顶堆）完成对元素的排序，这个⼤顶堆是以vector为表现形式的

complete binary tree（完全⼆叉树）。


什么是堆呢？


**堆是⼀棵完全⼆叉树，树中每个结点的值都不⼩于（或不⼤于）其左右孩⼦的值。** 如果⽗亲结点是⼤于等于左右孩

⼦就是⼤顶堆，⼩于等于左右孩⼦就是⼩顶堆。


所以⼤家经常说的⼤顶堆（堆头是最⼤元素），⼩顶堆（堆头是最⼩元素），如果懒得⾃⼰实现的话，就直接⽤

priority_queue（优先级队列）就可以了，底层实现都是⼀样的，从⼩到⼤排就是⼩顶堆，从⼤到⼩排就是⼤顶

堆。


本题我们就要使⽤优先级队列来对部分频率进⾏排序。


为什么不⽤快排呢， 使⽤快排要将map转换为vector的结构，然后对整个数组进⾏排序， ⽽这种场景下，我们其

实只需要维护k个有序的序列就可以了，所以使⽤优先级队列是最优的。


此时要思考⼀下，是使⽤⼩顶堆呢，还是⼤顶堆？


有的同学⼀想，题⽬要求前 K 个⾼频元素，那么果断⽤⼤顶堆啊。


那么问题来了，定义⼀个⼤⼩为k的⼤顶堆，在每次移动更新⼤顶堆的时候，每次弹出都把最⼤的元素弹出去了，

那么怎么保留下来前K个⾼频元素呢。


⽽且使⽤⼤顶堆就要把所有元素都进⾏排序，那能不能只排序k个元素呢？


**所以我们要⽤⼩顶堆，因为要统计最⼤前k个元素，只有⼩顶堆每次将最⼩的元素弹出，最后⼩顶堆⾥积累的才是**

**前k个最⼤元素。**


寻找前k个最⼤元素流程如图所示：（图中的频率只有三个，所以正好构成⼀个⼤⼩为3的⼩顶堆，如果频率更多⼀

些，则⽤这个⼩顶堆进⾏扫描）


我们来看⼀下C++代码：


class Solution {


public:

// ⼩顶堆


class mycomparison {


public:


bool operator()(const pair<int, int>& lhs, const pair<int, int>& rhs) {


return lhs.second        - rhs.second;


}


};


vector<int> topKFrequent(vector<int>& nums, int k) {

// 要统计元素出现频率

unordered_map<int, int> map; // map<nums[i],对应出现的次数>


for (int i = 0; i < nums.size(); i++) {


map[nums[i]]++;


}


// 对频率排序

// 定义⼀个⼩顶堆，⼤⼩为k


priority_queue<pair<int, int>, vector<pair<int, int>>, mycomparison> pri_que;


// ⽤固定⼤⼩为k的⼩顶堆，扫⾯所有频率的数值


for (unordered_map<int, int>::iterator it = map.begin(); it != map.end(); it++)


{


pri_que.push(*it);

if (pri_que.size() > k) { // 如果堆的⼤⼩⼤于了K，则队列弹出，保证堆的⼤⼩⼀直为k


pri_que.pop();


}


}


// 找出前K个⾼频元素，因为⼩顶堆先弹出的是最⼩的，所以倒序来输出到数组


vector<int> result(k);


for (int i = k      - 1; i >= 0; i--) {


result[i] = pri_que.top().first;


pri_que.pop();


}


return result;


}


};


时间复杂度: O(nlogk)

空间复杂度: O(n)

## **拓展**


⼤家对这个⽐较运算在建堆时是如何应⽤的，为什么左⼤于右就会建⽴⼩顶堆，反⽽建⽴⼤顶堆⽐较困惑。


确实 例如我们在写快排的cmp函数的时候， return left>right 就是从⼤到⼩， return left<right 就是从

⼩到⼤。


优先级队列的定义正好反过来了，可能和优先级队列的源码实现有关（我没有仔细研究），我估计是底层实现上优

先队列队⾸指向后⾯，队尾指向最前⾯的缘故！


# **9. 栈与队列总结篇**
## **栈与队列的理论基础**

[⾸先我们在栈与队列：来看看栈和队列不为⼈知的⼀⾯中讲解了栈和队列的理论基础。](https://programmercarl.com/%E6%A0%88%E4%B8%8E%E9%98%9F%E5%88%97%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)


⾥⾯提到了灵魂四问：


1. C++中stack，queue 是容器么？

2. 我们使⽤的stack，queue是属于那个版本的STL？

3. 我们使⽤的STL中stack，queue是如何实现的？

4. stack，queue 提供迭代器来遍历空间么？


相信不仅仅是C++中有这些问题，那么⼤家使⽤其他编程语⾔，也可以考虑⼀下这四个问题，栈和队列是如何实现

的。


栈与队列是我们熟悉的不能再熟悉的数据结构，但它们的底层实现，很多同学都⽐较模糊，这其实就是基础所在。


可以出⼀道⾯试题：栈⾥⾯的元素在内存中是连续分布的么？


这个问题有两个陷阱：


陷阱1：栈是容器适配器，底层容器使⽤不同的容器，导致栈内数据在内存中是不是连续分布。

陷阱2：缺省情况下，默认底层容器是deque，那么deque的在内存中的数据分布是什么样的呢？ 答案是：不

连续的，下⽂也会提到deque。


所以这就是考察候选者基础知识扎不扎实的好问题。


⼤家还是要多多重视起来！


[了解了栈与队列基础之后，那么可以⽤栈与队列：栈实现队列](https://programmercarl.com/0232.%E7%94%A8%E6%A0%88%E5%AE%9E%E7%8E%B0%E9%98%9F%E5%88%97.html) 和 [栈与队列：队列实现栈](https://programmercarl.com/0225.%E7%94%A8%E9%98%9F%E5%88%97%E5%AE%9E%E7%8E%B0%E6%A0%88.html) 来练习⼀下栈与队列的基

本操作。


[值得⼀提的是，⽤栈与队列：⽤队列实现栈还有点别扭中，其实只⽤⼀个队列就够了。](https://programmercarl.com/0225.%E7%94%A8%E9%98%9F%E5%88%97%E5%AE%9E%E7%8E%B0%E6%A0%88.html)


**⼀个队列在模拟栈弹出元素的时候只要将队列头部的元素（除了最后⼀个元素外）** **重新添加到队列尾部，此时在去**

**弹出元素就是栈的顺序了。**

## **栈经典题⽬**

### **栈在系统中的应⽤**


如果还记得编译原理的话，编译器在 词法分析的过程中处理括号、花括号等这个符号的逻辑，就是使⽤了栈这种数

据结构。


再举个例⼦，linux系统中，cd这个进⼊⽬录的命令我们应该再熟悉不过了。





这个命令最后进⼊a⽬录，系统是如何知道进⼊了a⽬录呢 ，这就是栈的应⽤。 **这在leetcode上也是⼀道题⽬，编**

**号：71. 简化路径，⼤家有空可以做⼀下。**


**递归的实现是栈：每⼀次递归调⽤都会把函数的局部变量、参数值和返回地址等压⼊调⽤栈中** ，然后递归返回的时

候，从栈顶弹出上⼀次递归的各项参数，所以这就是递归为什么可以返回上⼀层位置的原因。


所以栈在计算机领域中应⽤是⾮常⼴泛的。


有的同学经常会想学的这些数据结构有什么⽤，也开发不了什么软件，⼤多数同学说的软件应该都是可视化的软件

例如APP、⽹站之类的，那都是⾮常上层的应⽤了，底层很多功能的实现都是基础的数据结构和算法。


**所以数据结构与算法的应⽤往往隐藏在我们看不到的地⽅！**

### **括号匹配问题**


[在栈与队列：系统中处处都是栈的应⽤中我们讲解了括号匹配问题。](https://programmercarl.com/0020.%E6%9C%89%E6%95%88%E7%9A%84%E6%8B%AC%E5%8F%B7.html)


**括号匹配是使⽤栈解决的经典问题。**


建议要写代码之前要分析好有哪⼏种不匹配的情况，如果不动⼿之前分析好，写出的代码也会有很多问题。


先来分析⼀下 这⾥有三种不匹配的情况，


1. 第⼀种情况，字符串⾥左⽅向的括号多余了 ，所以不匹配。

2. 第⼆种情况，括号没有多余，但是 括号的类型没有匹配上。

3. 第三种情况，字符串⾥右⽅向的括号多余了，所以不匹配。


这⾥还有⼀些技巧，在匹配左括号的时候，右括号先⼊栈，就只需要⽐较当前元素和栈顶相不相等就可以了，⽐左

括号先⼊栈代码实现要简单的多了！

### **字符串去重问题**


[在栈与队列：匹配问题都是栈的强项中讲解了字符串去重问题。](https://programmercarl.com/1047.%E5%88%A0%E9%99%A4%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E6%89%80%E6%9C%89%E7%9B%B8%E9%82%BB%E9%87%8D%E5%A4%8D%E9%A1%B9.html)


047. 删除字符串中的所有相邻重复项


思路就是可以把字符串顺序放到⼀个栈中，然后如果相同的话 栈就弹出，这样最后栈⾥剩下的元素都是相邻不相同

的元素了。

### **逆波兰表达式问题**


[在栈与队列：有没有想过计算机是如何处理表达式的？中讲解了求逆波兰表达式。](https://programmercarl.com/0150.%E9%80%86%E6%B3%A2%E5%85%B0%E8%A1%A8%E8%BE%BE%E5%BC%8F%E6%B1%82%E5%80%BC.html)


本题中每⼀个⼦表达式要得出⼀个结果，然后拿这个结果再进⾏运算，那么 **这岂不就是⼀个相邻字符串消除的过**

**[程，和栈与队列：匹配问题都是栈的强项中的对对碰游戏是不是就⾮常像了。](https://programmercarl.com/1047.%E5%88%A0%E9%99%A4%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%AD%E7%9A%84%E6%89%80%E6%9C%89%E7%9B%B8%E9%82%BB%E9%87%8D%E5%A4%8D%E9%A1%B9.html)**

## **队列的经典题⽬**

### **滑动窗⼝最⼤值问题**


[在栈与队列：滑动窗⼝⾥求最⼤值引出⼀个重要数据结构中讲解了⼀种数据结构：单调队列。](https://programmercarl.com/0239.%E6%BB%91%E5%8A%A8%E7%AA%97%E5%8F%A3%E6%9C%80%E5%A4%A7%E5%80%BC.html)


这道题⽬还是⽐较绕的，如果第⼀次遇到这种题⽬，需要反复琢磨琢磨


主要思想是 **队列没有必要维护窗⼝⾥的所有元素，只需要维护有可能成为窗⼝⾥最⼤值的元素就可以了，同时保证**

**队列⾥的元素数值是由⼤到⼩的。**


那么这个维护元素单调递减的队列就叫做 **单调队列，即单调递减或单调递增的队列。C++中没有直接⽀持单调队**

**列，需要我们⾃⼰来⼀个单调队列**


⽽且 **不要以为实现的单调队列就是** **对窗⼝⾥⾯的数进⾏排序，如果排序的话，那和优先级队列⼜有什么区别了呢。**


设计单调队列的时候，pop，和push操作要保持如下规则：


1. pop(value)：如果窗⼝移除的元素value等于单调队列的出⼝元素，那么队列弹出元素，否则不⽤任何操作

2. push(value)：如果push的元素value⼤于⼊⼝元素的数值，那么就将队列出⼝的元素弹出，直到push元素的

数值⼩于等于队列⼊⼝元素的数值为⽌


保持如上规则，每次窗⼝移动的时候，只要问que.front()就可以返回当前窗⼝的最⼤值。


⼀些同学还会对单调队列都有⼀些困惑，⾸先要明确的是， **题解中单调队列⾥的pop和push接⼝，仅适⽤于本**

**题。**


**单调队列不是⼀成不变的，⽽是不同场景不同写法** ，总之要保证队列⾥单调递减或递增的原则，所以叫做单调队

列。


**不要以为本地中的单调队列实现就是固定的写法。**


我们⽤deque作为单调队列的底层数据结构，C++中deque是stack和queue默认的底层实现容器（这个我们之前已

经讲过），deque是可以两边扩展的，⽽且deque⾥元素并不是严格的连续分布的。

### **求前 K 个⾼频元素**


[在栈与队列：求前 K 个⾼频元素和队列有啥关系？中讲解了求前 K 个⾼频元素。](https://programmercarl.com/0347.%E5%89%8DK%E4%B8%AA%E9%AB%98%E9%A2%91%E5%85%83%E7%B4%A0.html)


通过求前 K 个⾼频元素，引出另⼀种队列就是 **优先级队列** 。


什么是优先级队列呢？


其实 **就是⼀个披着队列外⾐的堆** ，因为优先级队列对外接⼝只是从队头取元素，从队尾添加元素，再⽆其他取元素

的⽅式，看起来就是⼀个队列。


⽽且优先级队列内部元素是⾃动依照元素的权值排列。那么它是如何有序排列的呢？


缺省情况下priority_queue利⽤max-heap（⼤顶堆）完成对元素的排序，这个⼤顶堆是以vector为表现形式的

complete binary tree（完全⼆叉树）。


什么是堆呢？


**堆是⼀棵完全⼆叉树，树中每个结点的值都不⼩于（或不⼤于）其左右孩⼦的值。** 如果⽗亲结点是⼤于等于左右孩

⼦就是⼤顶堆，⼩于等于左右孩⼦就是⼩顶堆。


所以⼤家经常说的⼤顶堆（堆头是最⼤元素），⼩顶堆（堆头是最⼩元素），如果懒得⾃⼰实现的话，就直接⽤

priority_queue（优先级队列）就可以了，底层实现都是⼀样的，从⼩到⼤排就是⼩顶堆，从⼤到⼩排就是⼤顶

堆。


本题就要 **使⽤优先级队列来对部分频率进⾏排序。** 注意这⾥是对部分数据进⾏排序⽽不需要对所有数据排序！


所以排序的过程的时间复杂度是$O(\log k)$，整个算法的时间复杂度是$O(n\log k)$。

## **总结**


在栈与队列系列中，我们强调栈与队列的基础，也是很多同学容易忽视的点。


使⽤抽象程度越⾼的语⾔，越容易忽视其底层实现，⽽C++相对来说是⽐较接近底层的语⾔。


我们⽤栈实现队列，⽤队列实现栈来掌握的栈与队列的基本操作。


接着，通过括号匹配问题、字符串去重问题、逆波兰表达式问题来系统讲解了栈在系统中的应⽤，以及使⽤技巧。


通过求滑动窗⼝最⼤值，以及前K个⾼频元素介绍了两种队列：单调队列和优先级队列，这是特殊场景解决问题的

利器，是⼀定要掌握的。


好了，栈与队列我们就总结到这⾥了，接下来Carl就要带⼤家开启新的篇章了，⼤家加油！


