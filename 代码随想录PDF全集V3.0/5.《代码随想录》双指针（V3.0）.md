**[《代码随想录》作者：程序员Carl](https://github.com/youngyangyang04)**


代码随想录官⽹ [（网站持续更新优化内容，建议直接看网站）](https://www.programmercarl.com/) ：www.programmercarl.com

[代码随想录Github开源地址](https://github.com/youngyangyang04/leetcode-master)

[代码随想录算法公开课](https://programmercarl.com/other/gongkaike.html) [，代码随想录的全部内容将由我（程序员Carl）视频讲解并开免费开放给⼤家。](https://github.com/youngyangyang04)


[《代码随想录》](https://programmercarl.com/other/publish.html) 已经出版。


[代码随想录知识星球](https://programmercarl.com/other/kstar.html) 上万录友在这⾥学习


[代码随想录算法训练营](https://programmercarl.com/other/xunlianying.html) 帮助录友⾼效刷完代码随想录。


[微信公众号：代码随想录](https://img-blog.csdnimg.cn/74f47bc466234462bd2acc3ba6cd1b70.png)


[组队刷题，可以添加代码随想录官⽅微信](https://www.programmercarl.com/other/wechat.html)


[ACM模式练习，推荐：卡码⽹](https://kamacoder.com/)


**特别提示** ：PDF仅提供C++语⾔版本同时PDF中很多动图⽆法加载，其他编程语⾔版本和查看动图可以移步⾄代码

随想录官⽅⽹站查看。

# **1. 移除元素**


[⼒扣题⽬链接](https://leetcode.cn/problems/remove-element/)


给你⼀个数组 nums 和⼀个值 val，你需要 原地 移除所有数值等于 val 的元素，并返回移除后数组的新⻓度。


不要使⽤额外的数组空间，你必须仅使⽤ O(1) 额外空间并 **原地** 修改输⼊数组。


元素的顺序可以改变。你不需要考虑数组中超出新⻓度后⾯的元素。


示例 1:

给定 nums = [3,2,2,3], val = 3,

函数应该返回新的⻓度 2, 并且 nums 中的前两个元素均为 2。

你不需要考虑数组中超出新⻓度后⾯的元素。


示例 2:

给定 nums = [0,1,2,2,3,0,4,2], val = 2,

函数应该返回新的⻓度 5, 并且 nums 中的前五个元素为 0, 1, 3, 0, 4。


**你不需要考虑数组中超出新⻓度后⾯的元素。**

## **算法公开课**


**[《代码随想录》算法视频公开课：数组中移除元素并不容易！LeetCode：27. 移除元素，相信结合视频再看本篇](https://programmercarl.com/other/gongkaike.html)**

**题解，更有助于⼤家对本题的理解** 。

## **思路**


有的同学可能说了，多余的元素，删掉不就得了。


**要知道数组的元素在内存地址中是连续的，不能单独删除数组中的某个元素，只能覆盖。**


[数组的基础知识可以看这⾥程序员算法⾯试中，必须掌握的数组理论知识。](https://programmercarl.com/%E6%95%B0%E7%BB%84%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)

### **暴⼒解法**


这个题⽬暴⼒的解法就是两层for循环，⼀个for循环遍历数组元素 ，第⼆个for循环更新数组。


删除过程如下：


很明显暴⼒解法的时间复杂度是O(n^2)，这道题⽬暴⼒解法在leetcode上是可以过的。


代码如下：


// 时间复杂度：O(n^2)

// 空间复杂度：O(1)


class Solution {


public:


int removeElement(vector<int>& nums, int val) {


int size = nums.size();


for (int i = 0; i < size; i++) {


if (nums[i] == val) { // 发现需要移除的元素，就将数组集体向前移动⼀位


for (int j = i + 1; j < size; j++) {


nums[j            - 1] = nums[j];


}

i--; // 因为下标i以后的数值都向前移动了⼀位，所以i也向前移动⼀位

size--; // 此时数组的⼤⼩-1


}


}


return size;


}


};


时间复杂度：O(n^2)

空间复杂度：O(1)

### **双指针法**


双指针法（快慢指针法）： **通过⼀个快指针和慢指针在⼀个for循环下完成两个for循环的⼯作。**


定义快慢指针


快指针：寻找新数组的元素 ，新数组就是不含有⽬标元素的数组


慢指针：指向更新 新数组下标的位置


很多同学这道题⽬做的很懵，就是不理解 快慢指针究竟都是什么含义，所以⼀定要明确含义，后⾯的思路就更容易

理解了。


删除过程如下：


很多同学不了解


**双指针法（快慢指针法）在数组和链表的操作中是⾮常常⻅的，很多考察数组、链表、字符串等操作的⾯试题，都**

**使⽤双指针法。**


后续都会⼀⼀介绍到，本题代码如下：





注意这些实现⽅法并没有改变元素的相对位置！


时间复杂度：O(n)

空间复杂度：O(1)


/**

 - 相向双指针⽅法，基于元素顺序可以改变的题⽬描述改变了元素相对位置，确保了移动最少元素

 - 时间复杂度：O(n)

 - 空间复杂度：O(1)


*/


class Solution {


public:


int removeElement(vector<int>& nums, int val) {


int leftIndex = 0;


int rightIndex = nums.size() - 1;


while (leftIndex <= rightIndex) {

// 找左边等于val的元素


while (leftIndex <= rightIndex && nums[leftIndex] != val){


++leftIndex;


}

// 找右边不等于val的元素


while (leftIndex <= rightIndex && nums[rightIndex] == val) {


-- rightIndex;


}

// 将右边不等于val的元素覆盖左边等于val的元素


if (leftIndex < rightIndex) {


nums[leftIndex++] = nums[rightIndex--];


}


}


return leftIndex;  // leftIndex⼀定指向了最终数组末尾的下⼀个元素


}


};

## **相关题⽬推荐**


[26.删除排序数组中的重复项](https://leetcode.cn/problems/remove-duplicates-from-sorted-array/)

[283.移动零](https://leetcode.cn/problems/move-zeroes/)

[844.⽐较含退格的字符串](https://leetcode.cn/problems/backspace-string-compare/)

[977.有序数组的平⽅](https://leetcode.cn/problems/squares-of-a-sorted-array/)


打基础的时候，不要太迷恋于库函数。
# **2.反转字符串**


[⼒扣题⽬链接](https://leetcode.cn/problems/reverse-string/)


编写⼀个函数，其作⽤是将输⼊的字符串反转过来。输⼊字符串以字符数组 char[] 的形式给出。


不要给另外的数组分配额外的空间，你必须原地修改输⼊数组、使⽤ O(1) 的额外空间解决这⼀问题。


你可以假设数组中的所有字符都是 ASCII 码表中的可打印字符。


示例 1：

输⼊：["h","e","l","l","o"]

输出：["o","l","l","e","h"]


示例 2：

输⼊：["H","a","n","n","a","h"]

输出：["h","a","n","n","a","H"]

## **算法公开课**


**[《代码随想录》算法视频公开课：字符串基础操作！ | LeetCode：344.反转字符串，相信结合视频再看本篇题](https://programmercarl.com/other/gongkaike.html)**

**解，更有助于⼤家对本题的理解** 。

## **思路**


先说⼀说题外话：


对于这道题⽬⼀些同学直接⽤C++⾥的⼀个库函数 reverse，调⼀下直接完事了， 相信每⼀⻔编程语⾔都有这样的

库函数。


如果这么做题的话，这样⼤家不会清楚反转字符串的实现原理了。


但是也不是说库函数就不能⽤，是要分场景的。


如果在现场⾯试中，我们什么时候使⽤库函数，什么时候不要⽤库函数呢？


**如果题⽬关键的部分直接⽤库函数就可以解决，建议不要使⽤库函数。**


毕竟⾯试官⼀定不是考察你对库函数的熟悉程度， 如果使⽤python和java 的同学更需要注意这⼀点，因为

python、java提供的库函数⼗分丰富。


**如果库函数仅仅是** **解题过程中的⼀⼩部分，并且你已经很清楚这个库函数的内部实现原理的话，可以考虑使⽤库函**

**数。**


建议⼤家平时在leetcode上练习算法的时候本着这样的原则去练习，这样才有助于我们对算法的理解。


不要沉迷于使⽤库函数⼀⾏代码解决题⽬之类的技巧，不是说这些技巧不好，⽽是说这些技巧可以⽤来娱乐⼀下。


真正⾃⼰写的时候，要保证理解可以实现是相应的功能。


接下来再来讲⼀下如何解决反转字符串的问题。


[⼤家应该还记得，我们已经讲过了206.反转链表。](https://programmercarl.com/0206.%E7%BF%BB%E8%BD%AC%E9%93%BE%E8%A1%A8.html)


在反转链表中，使⽤了双指针的⽅法。


那么反转字符串依然是使⽤双指针的⽅法，只不过对于字符串的反转，其实要⽐链表简单⼀些。


因为字符串也是⼀种数组，所以元素在内存中是连续分布，这就决定了反转链表和反转字符串⽅式上还是有所差异

的。


[如果对数组和链表原理不清楚的同学，可以看这两篇，关于链表，你该了解这些！，必须掌握的数组理论知识。](https://programmercarl.com/%E9%93%BE%E8%A1%A8%E7%90%86%E8%AE%BA%E5%9F%BA%E7%A1%80.html)


对于字符串，我们定义两个指针（也可以说是索引下标），⼀个从字符串前⾯，⼀个从字符串后⾯，两个指针同时

向中间移动，并交换元素。


以字符串 hello 为例，过程如下：


不难写出如下C++代码:


循环⾥只要做交换s[i] 和s[j]操作就可以了，那么我这⾥使⽤了swap 这个库函数。⼤家可以使⽤。


因为相信⼤家都知道交换函数如何实现，⽽且这个库函数仅仅是解题中的⼀部分， 所以这⾥使⽤库函数也是可以

的。


swap可以有两种实现。


⼀种就是常⻅的交换数值：







⼀种就是通过位运算：





这道题⽬还是⽐较简单的，但是我正好可以通过这道题⽬说⼀说在刷题的时候，使⽤库函数的原则。


如果题⽬关键的部分直接⽤库函数就可以解决，建议不要使⽤库函数。


如果库函数仅仅是 解题过程中的⼀⼩部分，并且你已经很清楚这个库函数的内部实现原理的话，可以考虑使⽤库函

数。


本着这样的原则，我没有使⽤reverse库函数，⽽使⽤swap库函数。


**在字符串相关的题⽬中，库函数对⼤家的诱惑⼒是⾮常⼤的，因为会有各种反转，切割取词之类的操作** ，这也是为

什么字符串的库函数这么丰富的原因。


相信⼤家本着我所讲述的原则来做字符串相关的题⽬，在选择库函数的⻆度上会有所原则，也会有所收获。


C++代码如下：






时间复杂度: O(n)

空间复杂度: O(1)

# **3. 剑指Offer 05.替换空格**


[⼒扣题⽬链接](https://leetcode.cn/problems/ti-huan-kong-ge-lcof/)


请实现⼀个函数，把字符串 s 中的每个空格替换成"%20"。


示例 1：

输⼊：s = "We are happy."

输出："We%20are%20happy."

## **思路**


如果想把这道题⽬做到极致，就不要只⽤额外的辅助空间了！


⾸先扩充数组到每个空格替换成"%20"之后的⼤⼩。


然后从后向前替换空格，也就是双指针法，过程如下：


i指向新⻓度的末尾，j指向旧⻓度的末尾。


有同学问了，为什么要从后向前填充，从前向后填充不⾏么？


从前向后填充就是O(n^2)的算法了，因为每次添加元素都要将添加元素之后的所有元素向后移动。


**其实很多数组填充类的问题，都可以先预先给数组扩容带填充后的⼤⼩，然后在从后向前进⾏操作。**


这么做有两个好处：


1. 不⽤申请新数组。

2. 从后向前填充元素，避免了从前向后填充元素时，每次添加元素都要将添加元素之后的所有元素向后移动的问

题。


时间复杂度，空间复杂度均超过100%的⽤户。


C++代码如下：


时间复杂度：O(n)

空间复杂度：O(1)


此时算上本题，我们已经做了七道双指针相关的题⽬了分别是：


[27.移除元素](https://programmercarl.com/0027.%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0.html)

[15.三数之和](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html)

[18.四数之和](https://programmercarl.com/0018.%E5%9B%9B%E6%95%B0%E4%B9%8B%E5%92%8C.html)

[206.翻转链表](https://programmercarl.com/0206.%E7%BF%BB%E8%BD%AC%E9%93%BE%E8%A1%A8.html)

[142.环形链表II](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)

[344.反转字符串](https://programmercarl.com/0344.%E5%8F%8D%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2.html)

## **拓展**


这⾥也给⼤家拓展⼀下字符串和数组有什么差别，


字符串是若⼲字符组成的有限序列，也可以理解为是⼀个字符数组，但是很多语⾔对字符串做了特殊的规定，接下

来我来说⼀说C/C++中的字符串。


在C语⾔中，把⼀个字符串存⼊⼀个数组时，也把结束符 '\0'存⼊数组，并以此作为该字符串是否结束的标志。


例如这段代码：





在C++中，提供⼀个string类，string类会提供 size接⼝，可以⽤来判断string类字符串是否结束，就不⽤'\0'来判断

是否结束。


例如这段代码:





那么vector< char > 和 string ⼜有什么区别呢？


其实在基本操作上没有区别，但是 string提供更多的字符串处理的相关接⼝，例如string 重载了+，⽽vector却没

有。


所以想处理字符串，我们还是会定义⼀个string类型。


综合考察字符串操作的好题。
# **4.翻转字符串⾥的单词**


[⼒扣题⽬链接](https://leetcode.cn/problems/reverse-words-in-a-string/)


给定⼀个字符串，逐个翻转字符串中的每个单词。


示例 1：

输⼊: "the sky is blue"

输出: "blue is sky the"


示例 2：

输⼊: " hello world! "

输出: "world! hello"

解释: 输⼊字符串可以在前⾯或者后⾯包含多余的空格，但是反转后的字符不能包括。


示例 3：

输⼊: "a good  example"

输出: "example good a"

解释: 如果两个单词间有多余的空格，将反转后单词间的空格减少到只含⼀个。

## **算法公开课**


**[《代码随想录》算法视频公开课：字符串复杂操作拿捏了！ | LeetCode:151.翻转字符串⾥的单词，相信结合视频](https://programmercarl.com/other/gongkaike.html)**

**再看本篇题解，更有助于⼤家对本题的理解** 。

## **思路**


**这道题⽬可以说是综合考察了字符串的多种操作。**


⼀些同学会使⽤split库函数，分隔单词，然后定义⼀个新的string字符串，最后再把单词倒序相加，那么这道题题

⽬就是⼀道⽔题了，失去了它的意义。


所以这⾥我还是提⾼⼀下本题的难度： **不要使⽤辅助空间，空间复杂度要求为O(1)。**


不能使⽤辅助空间之后，那么只能在原字符串上下功夫了。


想⼀下，我们将整个字符串都反转过来，那么单词的顺序指定是倒序了，只不过单词本身也倒序了，那么再把单词

反转⼀下，单词不就正过来了。


所以解题思路如下：


移除多余空格


将整个字符串反转


将每个单词反转


举个例⼦，源字符串为："the sky is  blue  "


移除多余空格 : "the sky is blue"

字符串反转："eulb si yks eht"

单词反转："blue is sky the"


这样我们就完成了翻转字符串⾥的单词。


思路很明确了，我们说⼀说代码的实现细节，就拿移除多余空格来说，⼀些同学会上来写如下代码：


void removeExtraSpaces(string& s) {


for (int i = s.size() - 1; i    - 0; i--) {


if (s[i] == s[i      - 1] && s[i] == ' ') {


s.erase(s.begin() + i);


}


}

// 删除字符串最后⾯的空格


if (s.size() > 0 && s[s.size() - 1] == ' ') {


s.erase(s.begin() + s.size() - 1);


}

// 删除字符串最前⾯的空格


if (s.size() > 0 && s[0] == ' ') {


s.erase(s.begin());


}


}


逻辑很简单，从前向后遍历，遇到空格了就erase。


如果不仔细琢磨⼀下erase的时间复杂度，还以为以上的代码是O(n)的时间复杂度呢。


想⼀下真正的时间复杂度是多少，⼀个erase本来就是O(n)的操作。


erase操作上⾯还套了⼀个for循环，那么以上代码移除冗余空格的代码时间复杂度为O(n^2)。


那么使⽤双指针法来去移除空格，最后resize（重新设置）⼀下字符串的⼤⼩，就可以做到O(n)的时间复杂度。





有的同学可能发现⽤erase来移除空格，在leetcode上性能也还⾏。主要是以下⼏点；：


1. leetcode上的测试集⾥，字符串的⻓度不够⻓，如果⾜够⻓，性能差距会⾮常明显。


2. leetcode的测程序耗时不是很准确的。


版本⼀的代码是⼀般的思考过程，就是 先移除字符串前的空格，再移除中间的，再移除后⾯部分。


[不过其实还可以优化，这部分和27.移除元素的逻辑是⼀样⼀样的，本题是移除空格，⽽ 27.移除元素](https://programmercarl.com/0027.%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0.html) 就是移除元

素。


所以代码可以写的很精简，⼤家可以看 如下 代码 removeExtraSpaces 函数的实现：





如果以上代码看不懂，建议先把 [27.移除元素这道题⽬做了，或者看视频讲解：数组中移除元素并不容易！](https://programmercarl.com/0027.%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0.html)

LeetCode：27. 移除元素 。


此时我们已经实现了removeExtraSpaces函数来移除冗余空格。


[还要实现反转字符串的功能，⽀持反转字符串⼦区间，这个实现我们分别在344.反转字符串和541.反转字符串II⾥](https://programmercarl.com/0344.%E5%8F%8D%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2.html)

已经讲过了。


代码如下：







整体代码如下：


class Solution {


public:

void reverse(string& s, int start, int end){ //翻转，区间写法：左闭右闭 []


for (int i = start, j = end; i < j; i++, j--) {


swap(s[i], s[j]);


}


}


void removeExtraSpaces(string& s) {//去除所有空格并在相邻单词之间添加空格, 快慢指针。

int slow = 0;  //整体思想参考https://programmercarl.com/0027.移除元素.html


for (int i = 0; i < s.size(); ++i) { //

if (s[i] != ' ') { //遇到⾮空格就处理，即删除所有空格。

if (slow != 0) s[slow++] = ' '; //⼿动控制空格，给单词之间添加空格。slow != 0

说明不是第⼀个单词，需要在单词前添加空格。

while (i < s.size() && s[i] != ' ') { //补上该单词，遇到空格说明单词结束。


s[slow++] = s[i++];


}


}


}

s.resize(slow); //slow的⼤⼩即为去除多余空格后的⼤⼩。


}


string reverseWords(string s) {

removeExtraSpaces(s); //去除多余空格，保证单词之间之只有⼀个空格，且字符串⾸尾没空格。


reverse(s, 0, s.size() - 1);

int start = 0; //removeExtraSpaces后保证第⼀个单词的开始下标⼀定是0。


for (int i = 0; i <= s.size(); ++i) {

if (i == s.size() || s[i] == ' ') { //到达空格或者串尾，说明⼀个单词结束。进⾏翻

转。

reverse(s, start, i          - 1); //翻转，注意是左闭右闭 []的翻转。

start = i + 1; //更新下⼀个单词的开始下标start


}


}


return s;


}


};


时间复杂度: O(n)

空间复杂度: O(1) 或 O(n)，取决于语⾔中字符串是否可变


反转链表的写法很简单，⼀些同学甚⾄可以背下来但过⼀阵就忘了该咋写，主要是因为没有理解真正的反转

过程。
# **5.反转链表**


[⼒扣题⽬链接](https://leetcode.cn/problems/reverse-linked-list/)


题意：反转⼀个单链表。


示例:

输⼊: 1->2->3->4->5->NULL

输出: 5->4->3->2->1->NULL


## **算法公开课**

**[《代码随想录》算法视频公开课：帮你拿下反转链表 | LeetCode：206.反转链表，相信结合视频再看本篇题解，](https://programmercarl.com/other/gongkaike.html)**

**更有助于⼤家对本题的理解** 。

## **思路**


如果再定义⼀个新的链表，实现链表元素的反转，其实这是对内存空间的浪费。


其实只需要改变链表的next指针的指向，直接将链表反转 ，⽽不⽤重新定义⼀个新的链表，如图所示:


之前链表的头节点是元素1， 反转之后头结点就是元素5 ，这⾥并没有添加或者删除节点，仅仅是改变next指针的

⽅向。


那么接下来看⼀看是如何反转的呢？


我们拿有示例中的链表来举例，如动画所示：（纠正：动画应该是先移动pre，在移动cur）


⾸先定义⼀个cur指针，指向头结点，再定义⼀个pre指针，初始化为null。


然后就要开始反转了，⾸先要把 cur->next 节点⽤tmp指针保存⼀下，也就是保存⼀下这个节点。


为什么要保存⼀下这个节点呢，因为接下来要改变 cur->next 的指向了，将cur->next 指向pre ，此时已经反转了

第⼀个节点了。


接下来，就是循环⾛如下代码逻辑了，继续移动pre和cur指针。


最后，cur 指针已经指向了null，循环结束，链表也反转完毕了。 此时我们return pre指针就可以了，pre指针就指

向了新的头结点。

### **双指针法**


class Solution {


public:


ListNode* reverseList(ListNode* head) {

ListNode* temp; // 保存cur的下⼀个节点


ListNode* cur = head;


ListNode* pre = NULL;


while(cur) {

temp = cur->next; // 保存⼀下 cur的下⼀个节点，因为接下来要改变cur->next

cur->next = pre; // 翻转操作

// 更新pre 和 cur指针


pre = cur;


cur = temp;


}


return pre;


}


};


时间复杂度: O(n)

空间复杂度: O(1)

### **递归法**


递归法相对抽象⼀些，但是其实和双指针法是⼀样的逻辑，同样是当cur为空的时候循环结束，不断将cur指向pre

的过程。


关键是初始化的地⽅，可能有的同学会不理解， 可以看到双指针法中初始化 cur = head，pre = NULL，在递归法

中可以从如下代码看出初始化的逻辑也是⼀样的，只不过写法变了。


具体可以看代码（已经详细注释）， **双指针法写出来之后，理解如下递归写法就不难了，代码逻辑都是⼀样的。**





时间复杂度: O(n), 要递归处理链表的每个节点

空间复杂度: O(n), 递归调⽤了 n 层栈空间


我们可以发现，上⾯的递归写法和双指针法实质上都是从前往后翻转指针指向，其实还有另外⼀种与双指针法不同

思路的递归写法：从后往前翻转指针指向。


具体代码如下（带详细注释）：


class Solution {


public:


ListNode* reverseList(ListNode* head) {

// 边缘条件判断


if(head == NULL) return NULL;


if (head->next == NULL) return head;


// 递归调⽤，翻转第⼆个节点开始往后的链表


ListNode *last = reverseList(head->next);

// 翻转头节点与第⼆个节点的指向


head->next->next = head;

// 此时的 head 节点为尾节点，next 需要指向 NULL


head->next = NULL;


return last;


}


};


时间复杂度: O(n)

空间复杂度: O(n)

## **其他解法**

### **使⽤虚拟头结点解决链表反转**


使⽤虚拟头结点，通过头插法实现链表的反转（不需要栈）




### **使⽤栈解决反转链表的问题**

⾸先将所有的结点⼊栈


然后创建⼀个虚拟虚拟头结点，让cur指向虚拟头结点。然后开始循环出栈，每出来⼀个元素，就把它加⼊到

以虚拟头结点为头结点的链表当中，最后返回即可。


public ListNode reverseList(ListNode head) {


采⽤这种⽅法需要注意⼀点。就是当整个出栈循环结束以后，cur正好指向原来链表的第⼀个结点，⽽此时结

点1中的next指向的是结点2，因此最后还需要 cur.next = null

# **6.删除链表的倒数第N个节点**


[⼒扣题⽬链接](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/)


给你⼀个链表，删除链表的倒数第 n 个结点，并且返回链表的头结点。


进阶：你能尝试使⽤⼀趟扫描实现吗？


示例 1：


输⼊：head = [1,2,3,4,5], n = 2

输出：[1,2,3,5]

示例 2：


输⼊：head = [1], n = 1

输出：[]

示例 3：


输⼊：head = [1,2], n = 1

输出：[1]

## **算法公开课**


**[《代码随想录》算法视频公开课：：链表遍历学清楚！ | LeetCode：19.删除链表倒数第N个节点，相信结合视频](https://programmercarl.com/other/gongkaike.html)**

**再看本篇题解，更有助于⼤家对链表的理解。**

## **思路**


双指针的经典应⽤，如果要删除倒数第n个节点，让fast移动n步，然后让fast和slow同时移动，直到fast指向链表

末尾。删掉slow所指向的节点就可以了。


思路是这样的，但要注意⼀些细节。


分为如下⼏步：


⾸先这⾥我推荐⼤家使⽤虚拟头结点，这样⽅便处理删除实际头结点的逻辑，如果虚拟头结点不清楚，可以看

这篇： [链表：听说⽤虚拟头节点会⽅便很多？](https://programmercarl.com/0203.%E7%A7%BB%E9%99%A4%E9%93%BE%E8%A1%A8%E5%85%83%E7%B4%A0.html)


定义fast指针和slow指针，初始值为虚拟头结点，如图：


fast⾸先⾛n + 1步 ，为什么是n+1呢，因为只有这样同时移动的时候slow才能指向删除节点的上⼀个节点

（⽅便做删除操作），如图：


fast和slow同时移动，直到fast指向末尾，如题：


删除slow指向的下⼀个节点，如图：


此时不难写出如下C++代码：


class Solution {


public:


ListNode* removeNthFromEnd(ListNode* head, int n) {


ListNode* dummyHead = new ListNode(0);


dummyHead->next = head;


ListNode* slow = dummyHead;


ListNode* fast = dummyHead;


while(n-- && fast != NULL) {


fast = fast->next;


}

fast = fast->next; // fast再提前⾛⼀步，因为需要让slow指向删除节点的上⼀个节点


while (fast != NULL) {


fast = fast->next;


slow = slow->next;


}


slow->next = slow->next->next;


// ListNode *tmp = slow->next; C++释放内存的逻辑


// slow->next = tmp->next;


// delete nth;


return dummyHead->next;


}


};


时间复杂度: O(n)

空间复杂度: O(1)

# **7. ⾯试题 02.07. 链表相交**


同：160.链表相交


[⼒扣题⽬链接](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)


给你两个单链表的头节点 headA 和 headB ，请你找出并返回两个单链表相交的起始节点。如果两个链表没有交

点，返回 null 。


图示两个链表在节点 c1 开始相交：


题⽬数据 保证 整个链式结构中不存在环。


注意，函数返回结果后，链表必须 保持其原始结构 。


示例 1：


示例 2：


示例 3：


## **思路**

简单来说，就是求两个链表交点节点的 **指针** 。 这⾥同学们要注意，交点不是数值相等，⽽是指针相等。


为了⽅便举例，假设节点元素数值相等，则节点指针相等。


看如下两个链表，⽬前curA指向链表A的头结点，curB指向链表B的头结点：


我们求出两个链表的⻓度，并求出两个链表⻓度的差值，然后让curA移动到，和curB 末尾对⻬的位置，如图：


此时我们就可以⽐较curA和curB是否相同，如果不相同，同时向后移动curA和curB，如果遇到curA == curB，则

找到交点。


否则循环退出返回空指针。


C++代码如下：


class Solution {


public:


ListNode *getIntersectionNode(ListNode *headA, ListNode *headB) {


ListNode* curA = headA;


ListNode* curB = headB;


int lenA = 0, lenB = 0;

while (curA != NULL) { // 求链表A的⻓度


lenA++;


curA = curA->next;


}

while (curB != NULL) { // 求链表B的⻓度


lenB++;


curB = curB->next;


}


curA = headA;


curB = headB;

// 让curA为最⻓链表的头，lenA为其⻓度


if (lenB      - lenA) {


swap (lenA, lenB);


swap (curA, curB);


}

// 求⻓度差


int gap = lenA      - lenB;

// 让curA和curB在同⼀起点上（末尾位置对⻬）


while (gap--) {


curA = curA->next;


}

// 遍历curA 和 curB，遇到相同则直接返回


while (curA != NULL) {


if (curA == curB) {


return curA;


}


curA = curA->next;


curB = curB->next;


}


return NULL;


}


};


时间复杂度：O(n + m)

空间复杂度：O(1)


找到有没有环已经很不容易了，还要让我找到环的⼊⼝?
# **8.环形链表II**


[⼒扣题⽬链接](https://leetcode.cn/problems/linked-list-cycle-ii/)


题意：

给定⼀个链表，返回链表开始⼊环的第⼀个节点。 如果链表⽆环，则返回 null。


为了表示给定链表中的环，使⽤整数 pos 来表示链表尾连接到链表中的位置（索引从 0 开始）。 如果 pos 是 -1，

则在该链表中没有环。


**说明** ：不允许修改给定的链表。


## **算法公开课**

**[《代码随想录》算法视频公开课：把环形链表讲清楚！| LeetCode:142.环形链表II，相信结合视频在看本篇题](https://programmercarl.com/other/gongkaike.html)**

**解，更有助于⼤家对链表的理解。**

## **思路**


这道题⽬，不仅考察对链表的操作，⽽且还需要⼀些数学运算。


主要考察两知识点：


判断链表是否环


如果有环，如何找到这个环的⼊⼝

### **判断链表是否有环**


可以使⽤快慢指针法，分别定义 fast 和 slow 指针，从头结点出发，fast指针每次移动两个节点，slow指针每次移

动⼀个节点，如果 fast 和 slow指针在途中相遇 ，说明这个链表有环。


为什么fast ⾛两个节点，slow⾛⼀个节点，有环的话，⼀定会在环内相遇呢，⽽不是永远的错开呢


⾸先第⼀点： **fast指针⼀定先进⼊环中，如果fast指针和slow指针相遇的话，⼀定是在环中相遇，这是毋庸置疑**

**的。**


那么来看⼀下， **为什么fast指针和slow指针⼀定会相遇呢？**


可以画⼀个环，然后让 fast指针在任意⼀个节点开始追赶slow指针。


会发现最终都是这种情况， 如下图：


fast和slow各⾃再⾛⼀步， fast和slow就相遇了


这是因为fast是⾛两步，slow是⾛⼀步， **其实相对于slow来说，fast是⼀个节点⼀个节点的靠近slow的** ，所以fast

⼀定可以和slow重合。


动画如下：


### **如果有环，如何找到这个环的⼊⼝**

**此时已经可以判断链表是否有环了，那么接下来要找这个环的⼊⼝了。**


假设从头结点到环形⼊⼝节点 的节点数为x。

环形⼊⼝节点到 fast指针与slow指针相遇节点 节点数为y。

从相遇节点 再到环形⼊⼝节点节点数为 z。 如图所示：


那么相遇时：

slow指针⾛过的节点数为: x + y ，

fast指针⾛过的节点数： x + y + n (y + z) ，n为fast指针在环内⾛了n圈才遇到slow指针， （y+z）为 ⼀圈内

节点的个数A。


因为fast指针是⼀步⾛两个节点，slow指针⼀步⾛⼀个节点， 所以 fast指针⾛过的节点数 = slow指针⾛过的节点数

- 2：


(x + y) * 2 = x + y + n (y + z)


两边消掉⼀个（x+y）: x + y = n (y + z)


因为要找环形的⼊⼝，那么要求的是x，因为x表示 头结点到 环形⼊⼝节点的的距离。


所以要求x ，将x单独放在左⾯： x = n (y + z) - y,


再从n(y+z)中提出⼀个 （y+z）来，整理公式之后为如下公式： x = (n - 1) (y + z) + z 注意这⾥n⼀定是⼤

于等于1的，因为 fast指针⾄少要多⾛⼀圈才能相遇slow指针。


这个公式说明什么呢？


先拿n为1的情况来举例，意味着fast指针在环形⾥转了⼀圈之后，就遇到了 slow指针了。


当 n为1的时候，公式就化解为 x = z ，


这就意味着， **从头结点出发⼀个指针，从相遇节点** **也出发⼀个指针，这两个指针每次只⾛⼀个节点，** **那么当这两**

**个指针相遇的时候就是** **环形⼊⼝的节点** 。


也就是在相遇节点处，定义⼀个指针index1，在头结点处定⼀个指针index2。


让index1和index2同时移动，每次移动⼀个节点， 那么他们相遇的地⽅就是 环形⼊⼝的节点。


动画如下：


那么 n如果⼤于1是什么情况呢，就是fast指针在环形转n圈之后才遇到 slow指针。


其实这种情况和n为1的时候 效果是⼀样的，⼀样可以通过这个⽅法找到 环形的⼊⼝节点，只不过，index1 指针在

环⾥ 多转了(n-1)圈，然后再遇到index2，相遇点依然是环形的⼊⼝节点。


代码如下：


/**


  - Definition for singly-linked list.


  - struct ListNode {


  -  int val;


  -  ListNode *next;


  -  ListNode(int x) : val(x), next(NULL) {}


  - };


*/


class Solution {


public:


ListNode *detectCycle(ListNode *head) {


ListNode* fast = head;


ListNode* slow = head;


while(fast != NULL && fast->next != NULL) {


slow = slow->next;


fast = fast->next->next;

// 快慢指针相遇，此时从head 和 相遇点，同时查找直⾄相遇


if (slow == fast) {


ListNode* index1 = fast;


ListNode* index2 = head;


while (index1 != index2) {


index1 = index1->next;


index2 = index2->next;


}

return index2; // 返回环的⼊⼝


}


}


return NULL;


}


};


时间复杂度: O(n)，快慢指针相遇前，指针⾛的次数⼩于链表⻓度，快慢指针相遇后，两个index指针⾛的次

数也⼩于链表⻓度，总体为⾛的次数⼩于 2n

空间复杂度: O(1)

### **补充**


在推理过程中，⼤家可能有⼀个疑问就是： **为什么第⼀次在环中相遇，slow的** **步数** **是 x+y ⽽不是 x + 若⼲环的⻓**

**度 + y 呢？**


[即⽂章链表：环找到了，那⼊⼝呢？中如下的地⽅：](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)


⾸先slow进环的时候，fast⼀定是先进环来了。


如果slow进环⼊⼝，fast也在环⼊⼝，那么把这个环展开成直线，就是如下图的样⼦：


可以看出如果slow 和 fast同时在环⼊⼝开始⾛，⼀定会在环⼊⼝3相遇，slow⾛了⼀圈，fast⾛了两圈。


重点来了，slow进环的时候，fast⼀定是在环的任意⼀个位置，如图：


那么fast指针⾛到环⼊⼝3的时候，已经⾛了k + n 个节点，slow相应的应该⾛了(k + n) / 2 个节点。


因为k是⼩于n的（图中可以看出），所以(k + n) / 2 ⼀定⼩于n。


**也就是说slow⼀定没有⾛到环⼊⼝3，⽽fast已经到环⼊⼝3了** 。


这说明什么呢？


**在slow开始⾛的那⼀环已经和fast相遇了** 。


那有同学⼜说了，为什么fast不能跳过去呢？ 在刚刚已经说过⼀次了， **fast相对于slow是⼀次移动⼀个节点，所以**

**不可能跳过去** 。


好了，这次把为什么第⼀次在环中相遇，slow的 步数 是 x+y ⽽不是 x + 若⼲环的⻓度 + y ，⽤数学推理了⼀下，

[算是对链表：环找到了，那⼊⼝呢？的补充。](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)

## **总结**


这次可以说把环形链表这道题⽬的各个细节，完完整整的证明了⼀遍，说这是全⽹最详细讲解不为过吧，哈哈。


[⽤哈希表解决了两数之和，那么三数之和呢？](https://programmercarl.com/0001.%E4%B8%A4%E6%95%B0%E4%B9%8B%E5%92%8C.html)
# **9. 三数之和**


[⼒扣题⽬链接](https://leetcode.cn/problems/3sum/)


给你⼀个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出

所有满⾜条件且不重复的三元组。


**注意：** 答案中不可以包含重复的三元组。


示例：


给定数组 nums = [-1, 0, 1, 2, -1, -4]，


满⾜要求的三元组集合为：

[

[-1, 0, 1],

[-1, -1, 2]

]

## **算法公开课**


**[《代码随想录》算法视频公开课：梦破碎的地⽅！| LeetCode：15.三数之和，相信结合视频再看本篇题解，更有](https://programmercarl.com/other/gongkaike.html)**

**助于⼤家对本题的理解** 。


**注意[0， 0， 0， 0] 这组数据**

## **思路**

### **哈希解法**


两层for循环就可以确定 a 和b 的数值了，可以使⽤哈希法来确定 0-(a+b) 是否在 数组⾥出现过，其实这个思路是正

确的，但是我们有⼀个⾮常棘⼿的问题，就是题⽬中说的不可以包含重复的三元组。


把符合条件的三元组放进vector中，然后再去重，这样是⾮常费时的，很容易超时，也是这道题⽬通过率如此之低

的根源所在。


去重的过程不好处理，有很多⼩细节，如果在⾯试中很难想到位。


时间复杂度可以做到O(n^2)，但还是⽐较费时的，因为不好做剪枝操作。


⼤家可以尝试使⽤哈希法写⼀写，就知道其困难的程度了。


哈希法C++代码:


class Solution {


public:


vector<vector<int>> threeSum(vector<int>& nums) {


vector<vector<int>> result;


sort(nums.begin(), nums.end());

// 找出a + b + c = 0


// a = nums[i], b = nums[j], c = -(a + b)


for (int i = 0; i < nums.size(); i++) {

// 排序之后如果第⼀个元素已经⼤于零，那么不可能凑成三元组


if (nums[i] > 0) {


break;


}

if (i        - 0 && nums[i] == nums[i        - 1]) { //三元组元素a去重


continue;


}


unordered_set<int> set;


for (int j = i + 1; j < nums.size(); j++) {


if (j          - i + 2


&& nums[j] == nums[j-1]

&& nums[j-1] == nums[j-2]) { // 三元组元素b去重


continue;


}


int c = 0          - (nums[i] + nums[j]);


if (set.find(c) != set.end()) {


result.push_back({nums[i], nums[j], c});

set.erase(c);// 三元组元素c去重


} else {


set.insert(nums[j]);


}


}


}


return result;


}


};


时间复杂度: O(n^2)

空间复杂度: O(n)，额外的 set 开销

### **双指针**


**其实这道题⽬使⽤哈希法并不⼗分合适** ，因为在去重的操作中有很多细节需要注意，在⾯试中很难直接写出没有

bug的代码。


⽽且使⽤哈希法 在使⽤两层for循环的时候，能做的剪枝操作很有限，虽然时间复杂度是O(n^2)，也是可以在

leetcode上通过，但是程序的执⾏时间依然⽐较⻓ 。


接下来我来介绍另⼀个解法：双指针法， **这道题⽬使⽤双指针法** **要⽐哈希法⾼效⼀些** ，那么来讲解⼀下具体实现的

思路。


动画效果如下：


拿这个nums数组来举例，⾸先将数组排序，然后有⼀层for循环，i从下标0的地⽅开始，同时定⼀个下标left 定义

在i+1的位置上，定义下标right 在数组结尾的位置上。


依然还是在数组中找到 abc 使得a + b +c =0，我们这⾥相当于 a = nums[i]，b = nums[left]，c = nums[right]。


接下来如何移动left 和right呢， 如果nums[i] + nums[left] + nums[right] > 0 就说明 此时三数之和⼤了，因为数

组是排序后了，所以right下标就应该向左移动，这样才能让三数之和⼩⼀些。


如果 nums[i] + nums[left] + nums[right] < 0 说明 此时 三数之和⼩了，left 就向右移动，才能让三数之和⼤⼀

些，直到left与right相遇为⽌。


时间复杂度：O(n^2)。


C++代码代码如下：


class Solution {


public:


vector<vector<int>> threeSum(vector<int>& nums) {


vector<vector<int>> result;


sort(nums.begin(), nums.end());

// 找出a + b + c = 0


// a = nums[i], b = nums[left], c = nums[right]


for (int i = 0; i < nums.size(); i++) {

// 排序之后如果第⼀个元素已经⼤于零，那么⽆论如何组合都不可能凑成三元组，直接返回结果就可

以了


if (nums[i] > 0) {


return result;


}

// 错误去重a⽅法，将会漏掉-1,-1,2 这种情况


/*


if (nums[i] == nums[i + 1]) {


continue;


}


*/

// 正确去重a⽅法


if (i        - 0 && nums[i] == nums[i        - 1]) {


continue;


}


int left = i + 1;


int right = nums.size() - 1;


while (right        - left) {

// 去重复逻辑如果放在这⾥，0，0，0 的情况，可能直接导致 right<=left 了，从⽽漏掉了

0,0,0 这种三元组


/*


while (right > left && nums[right] == nums[right - 1]) right--;


while (right > left && nums[left] == nums[left + 1]) left++;


*/


if (nums[i] + nums[left] + nums[right] > 0) right--;


else if (nums[i] + nums[left] + nums[right] < 0) left++;


else {


result.push_back(vector<int>{nums[i], nums[left], nums[right]});

// 去重逻辑应该放在找到⼀个三元组之后，对b 和 c去重


while (right            - left && nums[right] == nums[right            - 1]) right--;


while (right            - left && nums[left] == nums[left + 1]) left++;


// 找到答案时，双指针同时收缩


right--;


left++;


}


}


}


return result;


}


};


时间复杂度: O(n^2)

空间复杂度: O(1)

### **去重逻辑的思考**

#### **a的去重**


说道去重，其实主要考虑三个数的去重。 a, b,c, 对应的就是 nums[i]，nums[left]，nums[right]


a 如果重复了怎么办，a是nums⾥遍历的元素，那么应该直接跳过去。


但这⾥有⼀个问题，是判断 nums[i] 与 nums[i + 1]是否相同，还是判断 nums[i] 与 nums[i-1] 是否相同。


有同学可能想，这不都⼀样吗。


其实不⼀样！


都是和 nums[i]进⾏⽐较，是⽐较它的前⼀个，还是⽐较他的后⼀个。


如果我们的写法是 这样：


那就我们就把 三元组中出现重复元素的情况直接pass掉了。 例如{-1, -1,2} 这组数据，当遍历到第⼀个-1 的时候，

判断 下⼀个也是-1，那这组数据就pass了。


**我们要做的是** **不能有重复的三元组，但三元组内的元素是可以重复的！**


所以这⾥是有两个重复的维度。


那么应该这么写：







这么写就是当前使⽤ nums[i]，我们判断前⼀位是不是⼀样的元素，在看 {-1, -1,2} 这组数据，当遍历到 第⼀个 -1

的时候，只要前⼀位没有-1，那么 {-1, -1,2} 这组数据⼀样可以收录到 结果集⾥。


这是⼀个⾮常细节的思考过程。

#### **b与c的去重**


很多同学写本题的时候，去重的逻辑多加了 对right 和left 的去重：（代码中注释部分）







但细想⼀下，这种去重其实对提升程序运⾏效率是没有帮助的。


拿right去重为例，即使不加这个去重逻辑，依然根据 while (right > left) 和 if (nums[i] + nums[left]

+ nums[right] > 0) 去完成right-- 的操作。


多加了 while (left < right && nums[right] == nums[right + 1]) right--; 这⼀⾏代码，其实就是把

需要执⾏的逻辑提前执⾏了，但并没有减少 判断的逻辑。


最直⽩的思考过程，就是right还是⼀个数⼀个数的减下去的，所以在哪⾥减的都是⼀样的。


所以这种去重 是可以不加的。 仅仅是 把去重的逻辑提前了⽽已。


## **思考题**

[既然三数之和可以使⽤双指针法，我们之前讲过的1.两数之和，可不可以使⽤双指针法呢？](https://programmercarl.com/0001.%E4%B8%A4%E6%95%B0%E4%B9%8B%E5%92%8C.html)


如果不能，题意如何更改就可以使⽤双指针法呢？ **⼤家留⾔说出⾃⼰的想法吧！**


两数之和 [就不能使⽤双指针法，因为1.两数之和要求返回的是索引下标，](https://programmercarl.com/0001.%E4%B8%A4%E6%95%B0%E4%B9%8B%E5%92%8C.html) ⽽双指针法⼀定要排序，⼀旦排序之后

原数组的索引就被改变了。


[如果1.两数之和要求返回的是数值的话，就可以使⽤双指针法了。](https://programmercarl.com/0001.%E4%B8%A4%E6%95%B0%E4%B9%8B%E5%92%8C.html)


⼀样的道理，能解决四数之和

那么五数之和、六数之和、N数之和呢？
# **10. 四数之和**


[⼒扣题⽬链接](https://leetcode.cn/problems/4sum/)


题意：给定⼀个包含 n 个整数的数组 nums 和⼀个⽬标值 target，判断 nums 中是否存在四个元素 a，b，c 和 d

，使得 a + b + c + d 的值与 target 相等？找出所有满⾜条件且不重复的四元组。


**注意：**


答案中不可以包含重复的四元组。


示例：

给定数组 nums = [1, 0, -1, 0, -2, 2]，和 target = 0。

满⾜要求的四元组集合为：

[

[-1, 0, 0, 1],

[-2, -1, 1, 2],

[-2, 0, 0, 2]

]

## **算法公开课**


**[《代码随想录》算法视频公开课：难在去重和剪枝！| LeetCode：18. 四数之和，相信结合视频再看本篇题解，更](https://programmercarl.com/other/gongkaike.html)**

**有助于⼤家对本题的理解** 。

## **思路**


[四数之和，和15.三数之和是⼀个思路，都是使⽤双指针法, 基本解法就是在15.三数之和](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html) 的基础上再套⼀层for循

环。


但是有⼀些细节需要注意，例如： 不要判断 nums[k] > target 就返回了，三数之和 可以通过 nums[i] > 0 就

返回了，因为 0 已经是确定的数了，四数之和这道题⽬ target是任意值。⽐如：数组是 [-4, -3, -2,

-1] ， target 是 -10 ，不能因为 -4 > -10 ⽽跳过。但是我们依旧可以去做剪枝，逻辑变成 nums[i] > target

&& (nums[i] >=0 || target >= 0) 就可以了。


[15.三数之和的双指针解法是⼀层for循环num[i]为确定值，然后循环内有left和right下标作为双指针，找到nums[i]](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html)

+ nums[left] + nums[right] == 0。


四数之和的双指针解法是两层for循环nums[k] + nums[i]为确定值，依然是循环内有left和right下标作为双指针，

找出nums[k] + nums[i] + nums[left] + nums[right] == target的情况，三数之和的时间复杂度是O(n^2)，四数之

和的时间复杂度是O(n^3) 。


那么⼀样的道理，五数之和、六数之和等等都采⽤这种解法。


[对于15.三数之和双指针法就是将原本暴⼒O(n^3)的解法，降为O(n^2)的解法，四数之和的双指针解法就是将原本](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html)

暴⼒O(n^4)的解法，降为O(n^3)的解法。


[之前我们讲过哈希表的经典题⽬：454.四数相加II，相对于本题简单很多，因为本题是要求在⼀个集合中找出四个](https://programmercarl.com/0454.%E5%9B%9B%E6%95%B0%E7%9B%B8%E5%8A%A0II.html)

数相加等于target，同时四元组不能重复。


[⽽454.四数相加II是四个独⽴的数组，只要找到A[i] + B[j] + C[k] + D[l] = 0就可以，不⽤考虑有重复的四个元素相加](https://programmercarl.com/0454.%E5%9B%9B%E6%95%B0%E7%9B%B8%E5%8A%A0II.html)

等于0的情况，所以相对于本题还是简单了不少！


我们来回顾⼀下，⼏道题⽬使⽤了双指针法。


双指针法将时间复杂度：O(n^2)的解法优化为 O(n)的解法。也就是降⼀个数量级，题⽬如下：


[27.移除元素](https://programmercarl.com/0027.%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0.html)

[15.三数之和](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html)

[18.四数之和](https://programmercarl.com/0018.%E5%9B%9B%E6%95%B0%E4%B9%8B%E5%92%8C.html)


链表相关双指针题⽬：


[206.反转链表](https://programmercarl.com/0206.%E7%BF%BB%E8%BD%AC%E9%93%BE%E8%A1%A8.html)

[19.删除链表的倒数第N个节点](https://programmercarl.com/0019.%E5%88%A0%E9%99%A4%E9%93%BE%E8%A1%A8%E7%9A%84%E5%80%92%E6%95%B0%E7%AC%ACN%E4%B8%AA%E8%8A%82%E7%82%B9.html)

[⾯试题 02.07. 链表相交](https://programmercarl.com/%E9%9D%A2%E8%AF%95%E9%A2%9802.07.%E9%93%BE%E8%A1%A8%E7%9B%B8%E4%BA%A4.html)

[142题.环形链表II](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)


双指针法在字符串题⽬中还有很多应⽤，后⾯还会介绍到。


C++代码


class Solution {


public:


vector<vector<int>> fourSum(vector<int>& nums, int target) {


vector<vector<int>> result;


sort(nums.begin(), nums.end());


for (int k = 0; k < nums.size(); k++) {

// 剪枝处理


if (nums[k] > target && nums[k] >= 0) {

break; // 这⾥使⽤break，统⼀通过最后的return返回


}

// 对nums[k]去重


if (k        - 0 && nums[k] == nums[k        - 1]) {


continue;


}


for (int i = k + 1; i < nums.size(); i++) {

// 2级剪枝处理


if (nums[k] + nums[i] > target && nums[k] + nums[i] >= 0) {


break;


}


// 对nums[i]去重


if (i          - k + 1 && nums[i] == nums[i          - 1]) {


continue;


}


int left = i + 1;


int right = nums.size() - 1;


while (right          - left) {

// nums[k] + nums[i] + nums[left] + nums[right] > target 会溢出


if ((long) nums[k] + nums[i] + nums[left] + nums[right] > target) {


right--;

// nums[k] + nums[i] + nums[left] + nums[right] < target 会溢出


} else if ((long) nums[k] + nums[i] + nums[left] + nums[right] <


target) {


left++;


} else {


result.push_back(vector<int>{nums[k], nums[i], nums[left],


nums[right]});

// 对nums[left]和nums[right]去重


while (right              - left && nums[right] == nums[right              - 1]) right--;


while (right              - left && nums[left] == nums[left + 1]) left++;


// 找到答案时，双指针同时收缩


right--;


left++;


}


}


}


}


return result;


}


};


时间复杂度: O(n^3)

空间复杂度: O(1)

## **补充**


⼆级剪枝的部分：


可以优化为：







因为只要 nums[k] + nums[i] > target，那么 nums[i] 后⾯的数都是正数的话，就⼀定 不符合条件了。


不过这种剪枝 其实有点 ⼩绕，⼤家能够理解 ⽂章给的完整代码的剪枝 就够了。


⼜是⼀波总结


相信⼤家已经对双指针法很熟悉了，但是双指针法并不⾪属于某⼀种数据结构，我们在讲解数组，链表，字符串都

⽤到了双指针法，所有有必要针对双指针法做⼀个总结。
# **11. 双指针总结篇**
## **数组篇**


[在数组：就移除个元素很难么？中，原地移除数组上的元素，我们说到了数组上的元素，不能真正的删除，只能覆](https://programmercarl.com/0027.%E7%A7%BB%E9%99%A4%E5%85%83%E7%B4%A0.html)

盖。


⼀些同学可能会写出如下代码（伪代码）：







这个代码看上去好像是O(n)的时间复杂度，其实是O(n^2)的时间复杂度，因为erase操作也是O(n)的操作。


所以此时使⽤双指针法才展现出效率的优势： **通过两个指针在⼀个for循环下完成两个for循环的⼯作。**

## **字符串篇**


[在字符串：这道题⽬，使⽤库函数⼀⾏代码搞定中讲解了反转字符串，注意这⾥强调要原地反转，要不然就失去了](https://programmercarl.com/0344.%E5%8F%8D%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2.html)

题⽬的意义。


使⽤双指针法， **定义两个指针（也可以说是索引下标），⼀个从字符串前⾯，⼀个从字符串后⾯，两个指针同时向**

**中间移动，并交换元素。** ，时间复杂度是O(n)。


[在替换空格](https://programmercarl.com/%E5%89%91%E6%8C%87Offer05.%E6%9B%BF%E6%8D%A2%E7%A9%BA%E6%A0%BC.html) 中介绍使⽤双指针填充字符串的⽅法，如果想把这道题⽬做到极致，就不要只⽤额外的辅助空间了！


思路就是 **⾸先扩充数组到每个空格替换成"%20"之后的⼤⼩。然后双指针从后向前替换空格。**


有同学问了，为什么要从后向前填充，从前向后填充不⾏么？


从前向后填充就是O(n^2)的算法了，因为每次添加元素都要将添加元素之后的所有元素向后移动。


**其实很多数组（字符串）填充类的问题，都可以先预先给数组扩容带填充后的⼤⼩，然后在从后向前进⾏操作。**


[那么在字符串：花式反转还不够！中，我们使⽤双指针法，⽤O(n)的时间复杂度完成字符串删除类的操作，因为题](https://programmercarl.com/0151.%E7%BF%BB%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2%E9%87%8C%E7%9A%84%E5%8D%95%E8%AF%8D.html)

⽬要删除冗余空格。


**在删除冗余空格的过程中，如果不注意代码效率，很容易写成了O(n^2)的时间复杂度。其实使⽤双指针法O(n)就**

**可以搞定。**


**主要还是⼤家⽤erase⽤的⽐较随意，⼀定要注意for循环下⽤erase的情况，⼀般可以⽤双指针写效率更⾼！**

## **链表篇**


翻转链表是现场⾯试，⽩纸写代码的好题，考察了候选者对链表以及指针的熟悉程度，⽽且代码也不⻓，适合在⽩

纸上写。


[在链表：听说过两天反转链表⼜写不出来了？中，讲如何使⽤双指针法来翻转链表，](https://programmercarl.com/0206.%E7%BF%BB%E8%BD%AC%E9%93%BE%E8%A1%A8.html) **只需要改变链表的next指针的**

**指向，直接将链表反转** **，⽽不⽤重新定义⼀个新的链表。**


思路还是很简单的，代码也不⻓，但是想在⽩纸上⼀次性写出bugfree的代码，并不是容易的事情。


[在链表中求环，应该是双指针在链表⾥最经典的应⽤，在链表：环找到了，那⼊⼝呢？中讲解了如何通过双指针判](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)

断是否有环，⽽且还要找到环的⼊⼝。


**使⽤快慢指针（双指针法），分别定义 fast 和 slow指针，从头结点出发，fast指针每次移动两个节点，slow指针**

**每次移动⼀个节点，如果 fast 和 slow指针在途中相遇** **，说明这个链表有环。**


那么找到环的⼊⼝，其实需要点简单的数学推理，我在⽂章中把找环的⼊⼝清清楚楚的推理的⼀遍，如果对找环⼊

[⼝不够清楚的同学建议⾃⼰看⼀看链表：环找到了，那⼊⼝呢？。](https://programmercarl.com/0142.%E7%8E%AF%E5%BD%A2%E9%93%BE%E8%A1%A8II.html)
## **N数之和篇**


[在哈希表：解决了两数之和，那么能解决三数之和么？中，讲到使⽤哈希法可以解决1.两数之和的问题](https://programmercarl.com/0015.%E4%B8%89%E6%95%B0%E4%B9%8B%E5%92%8C.html)


其实使⽤双指针也可以解决1.两数之和的问题，只不过1.两数之和求的是两个元素的下标，没法⽤双指针，如果改

成求具体两个元素的数值就可以了，⼤家可以尝试⽤双指针做⼀个leetcode上两数之和的题⽬，就可以体会到我说

的意思了。


使⽤了哈希法解决了两数之和，但是哈希法并不使⽤于三数之和！


使⽤哈希法的过程中要把符合条件的三元组放进vector中，然后在去去重，这样是⾮常费时的，很容易超时，也是

三数之和通过率如此之低的根源所在。


去重的过程不好处理，有很多⼩细节，如果在⾯试中很难想到位。


时间复杂度可以做到O(n^2)，但还是⽐较费时的，因为不好做剪枝操作。


所以这道题⽬使⽤双指针法才是最为合适的，⽤双指针做这道题⽬才能就能真正体会到， **通过前后两个指针不算向**

**中间逼近，在⼀个for循环下完成两个for循环的⼯作。**


只⽤双指针法时间复杂度为O(n^2)，但⽐哈希法的O(n^2)效率⾼得多，哈希法在使⽤两层for循环的时候，能做的

剪枝操作很有限。


[在双指针法：⼀样的道理，能解决四数之和中，讲到了四数之和，其实思路是⼀样的，](https://programmercarl.com/0018.%E5%9B%9B%E6%95%B0%E4%B9%8B%E5%92%8C.html) **在三数之和的基础上再套⼀**

**层for循环，依然是使⽤双指针法。**


对于三数之和使⽤双指针法就是将原本暴⼒O(n^3)的解法，降为O(n^2)的解法，四数之和的双指针解法就是将原本

暴⼒O(n^4)的解法，降为O(n^3)的解法。


同样的道理，五数之和，n数之和都是在这个基础上累加。

## **总结**


本⽂中⼀共介绍了leetcode上九道使⽤双指针解决问题的经典题⽬，除了链表⼀些题⽬⼀定要使⽤双指针，其他题

⽬都是使⽤双指针来提⾼效率，⼀般是将O(n^2)的时间复杂度，降为$O(n)$。


建议⼤家可以把⽂中涉及到的题⽬在好好做⼀做，琢磨琢磨，基本对双指针法就不在话下了。


